# v2.5 — Spaces Completion + Design Foundation

**Status:** In progress (`feature/v2.5-spaces-completion`) — not yet tagged. This note is provisional and will be finalized when v2.5 ships.

## What actually shipped (so far)

- **One dashboard system.** Personal is now a first-class Space rendered through the shared `SpaceDashboard` shell; the legacy `DashboardClient` is retired. Doctrine: **Space → Template → Materialized Sections → Widgets**, hero as the only sanctioned per-Space divergence. See [../doctrine/spaces.md](../doctrine/spaces.md).
- **Multi-currency architecture (MC1)** — reporting currency on the Space, read-time conversion, historical FX, estimated-flag honesty. See [../doctrine/money-and-fx.md](../doctrine/money-and-fx.md).
- **Financial semantic spine (Phase 2 / P2-x)** — the canonical `Providers → identity/evidence → semantics → facts/projections → consumers` spine; Financial Doctrine Oracle green; zero remaining parallel semantic authorities. See [../doctrine/financial-semantics.md](../doctrine/financial-semantics.md).
- **Canonical SpaceData contracts** per workspace (Investments, Connections, Liquidity, Cash Flow, Debt) and the **SpaceDashboard decomposition** (SpaceShell + workspace registry + declarative loading). See [../systems/spaces.md](../systems/spaces.md).
- **Atlas design-system push** — the live Space surfaces rebuilt in the Atlas material language (perspective + rail-tab redesigns, shell nav), presentation-only.
- **Growth & Security platform wave** — public/legal site, beta-access system + `registration_mode`, Turnstile, auth-anomaly detector, Plaid webhook receiver, sync-lock hardening, SnapshotAmendment system, API usage/cost accounting, typed background-job platform (OPS-4/OPS-5). See [../systems/platform-ops.md](../systems/platform-ops.md).
- **Legacy `Account` physically retired** (migration `20260716120000`) — single-path canonical `SpaceAccountLink` + `FinancialAccount`.

## Known gaps

- `SpaceDashboard.tsx` host decomposition not fully complete (remaining SD-x phases).
- Atlas ratchet fence not yet expanded to all trees (a ruling + one slice remains).
- v2.5.5 Financial Intelligence convergence (DayFacts sole-fold, classifier v3, named net measures) is a follow-on closeout — see [../plans/ROADMAP.md](../plans/ROADMAP.md).

## Migration requirements

Additive throughout, plus the legacy-`Account` physical retirement (`20260716120000_retire_legacy_account_model`), which drops the legacy `Account` model + FK columns. Requires Phase-0 gates A/B/C/E = 0 (verified for local) and a Cascade-hazard-aware backup window in production. `VisibilityLevel.SHARED` enum-drop is a separate follow-on migration.

## Production readiness

Not ready for external users. Remaining is dominated by **verification / configuration / operations** (consent capture, LLM disclosure, Sentry, uptime monitor, backup drill, invite-only + Turnstile + Plaid production verification) rather than feature construction — see the production-readiness audit in [../audits/](../audits/).

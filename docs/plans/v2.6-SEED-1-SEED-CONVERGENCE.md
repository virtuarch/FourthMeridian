# v2.6-SEED-1 — seed convergence

The seed modelled a product that no longer exists. This measures the gap, moves
the demo world onto Spaces a user can actually create, and proves coverage did
not shrink.

⚠️ **All work was done against a throwaway database (`fintracker_seedwork`),
created and dropped inside this slice. The working database was never seeded.**
Why that mattered turned out to be the headline finding.

---

## 1. THE WIPE — the finding that outranked the naming work

`prisma/seed.ts` performs an **unscoped** wipe:

```ts
await prisma.spaceAccountLink.deleteMany();
await prisma.financialAccount.deleteMany();
await prisma.transaction.deleteMany();
await prisma.space.deleteMany();
await prisma.user.deleteMany();      // ← no filter
```

The working database holds a **real account** beside the demo world:

```
Chris Hogan <chr.hogan1997@gmail.com>   11 accounts · 4,112 transactions · 748 snapshots
```

`npx prisma db seed` would have deleted it. The existing guard
(`assertNonProductionSeed`) asks *where* we are — `NODE_ENV`, `VERCEL_ENV` — and
would have raised no objection, because the environment was correct. It cannot
ask *whose data is here*.

**Fixed.** `assertOnlyDemoUsersPresent()` refuses when any user is not one of the
four demo accounts, and names them:

```
[seed] REFUSING TO RUN — this database contains account(s) the seed does not own:
         chr.hogan1997@gmail.com

       prisma/seed.ts wipes EVERY table, unscoped, including User. Running it
       here would delete the account(s) above and everything they own.
```

Verified by running the seed against the working database: it refused, named the
account, and the data was confirmed intact afterwards. `SEED_ALLOW_DESTRUCTIVE=1`
still overrides — the same explicit opt-in `db-guard.ts` uses.

"Refuse and say who" is recoverable; "wipe and report success" is not.

---

## 2. The gap: the seed modelled a product nobody can build

Creatable today (`lib/space-templates/registry.ts`): **`family` and `custom`.**
Everything else is `comingSoon` (disabled) or `hidden` (retired).

The seed produced, before:

```
PERSONAL 2 · HOUSEHOLD 1 · DEBT_PAYOFF 1 · TRIP 1 · INVESTMENT 1 · BUSINESS 1 · PROPERTY 1
FAMILY 0 · CUSTOM 0
```

**Six of eight Spaces were retired categories, and neither creatable type
appeared at all.** Every measurement taken over that corpus — including several
in this arc's own reports — described a product shape a user cannot build.

---

## 3. The instrument: states, not passing audits

`scripts/audit-seed-coverage.ts` (INFORMATIONAL, registered).

The REQUIRED gate passes on a fresh seed, and that is almost meaningless on its
own: an invariant over a corpus that cannot express its failure mode passes
**vacuously**. So the acceptance contract for a reseed is not "the audits still
pass" — it is **"every architectural STATE still exists"**.

46 states measured across Space shape, roles, visibility, account types, flow
types, ownership, chronology, lifecycle, event identity, transfers, debt,
snapshots, goals and holdings.

---

## 4. Before / after

```
=== COVERAGE DIFF (before → after) ===
  IDENTICAL — every state preserved
```

All 46 states unchanged. Resulting corpus:

```
CUSTOM 5 · OTHER 4 (platform Spaces) · PERSONAL 2 · FAMILY 1
```

**Coverage preserved, names not.** The mapping:

| Was | Now | Why the coverage survives |
| --- | --- | --- |
| Jane's / John's Space `PERSONAL` | unchanged | not user-pickable, but every real account gets one — the live path |
| Smith-Doe Household `HOUSEHOLD` | **Smith-Doe Family** `FAMILY` | the one live SHARED template; keeps the 2-member shape and BALANCE_ONLY links |
| Debt Payoff Tracker `DEBT_PAYOFF` | Debt Paydown `CUSTOM` | debt accounts + DEBT_REDUCTION goals are account/goal facts, not category facts |
| Japan Trip 2027 `TRIP` | `CUSTOM` | the no-accounts Space (day-zero rendering) |
| Investment Club `INVESTMENT` | `CUSTOM` | 3 members incl. VIEWER; holdings |
| JD Freelance LLC `BUSINESS` | JD Freelance `CUSTOM` | the ADMIN role |
| Austin Home `PROPERTY` | `CUSTOM` | real-asset account types |

This works because **the category was never load-bearing for any invariant.** It
selected a section preset and a hero definition; the architectural states come
from members and accounts. So the shapes move across untouched and the retired
presets stop being exercised — which is the point.

Gate on the reseeded scratch DB: **18/18 REQUIRED audits pass.**

---

## 5. What the seed still cannot represent

Five states were **absent before and remain absent** — pre-existing gaps, not
regressions, and each one makes a REQUIRED invariant pass vacuously in CI:

| Absent state | The invariant it disarms |
| --- | --- |
| `economicDate ≠ posting date` (0 rows) | the entire L8-B economic-chronology cutover |
| tombstoned / soft-deleted rows (0) | **`audit-event-identity` cannot catch a stale projection** — exactly the v2.6-EVENT-1 defect |
| events with 2+ observations (0) | pending→posted as ONE event: the whole point of L8 |
| rows with a resolved counterparty (0) | the transfer authority's persisted verdict |
| estimated snapshots (0) | FX-estimated disclosure |

This is empirical confirmation of a claim made earlier in the arc on reasoning
alone: **CI genuinely cannot catch the Plaid `removed[]` reprojection defect**,
because no seeded corpus has ever contained a tombstoned row.

Also unrepresentable today:

- **Multi-currency.** One currency across all seeded accounts, so FX conversion,
  `fxMiss` exclusion and the `unconverted` disclosure are never exercised.
- **`SUMMARY_ONLY` visibility.** Only `FULL` and `BALANCE_ONLY` are seeded.
- **`CONTRADICTORY` lifecycle rows** — the chronology audit's refusal path.

Closing these is the single highest-value follow-up, and a reseed is the cheapest
moment to do it. It was **not** done here: this slice's contract was "coverage
must not shrink", and adding states is a different change that deserves its own
measurement.

---

## 6. Verification

- Coverage diff before/after: **identical**, 46/46 states
- **18/18 REQUIRED audits** on the reseeded scratch database
- 485/485 unit tests · typecheck clean · 0 lint errors on the working tree
- The seed **refuses** on the working database, verified, with the account intact
  afterwards
- Scratch database dropped

---

## Out of scope — recorded

1. **"Remove all remnants of decommissioned spaces" is only half done.** The seed
   no longer *creates* retired categories, but the retired categories themselves
   still exist across the codebase: `SpaceCategory` enum members, their
   `lib/space-presets.ts` section lists, `lib/space-hero.ts` definitions,
   `lib/perspectives.ts` perspective lists, and the `emergency_fund_progress`
   widget contract. That deletion is the v2.6-LEGACY-1 follow-up and needs the
   declared-baseline successor first (retiring `emergency_fund_progress` orphans
   `resolveExpenseBaseline`'s DECLARED rung).
2. **The five absent states** (§5) — the reseed that closes them.
3. **Platform Spaces use `category: OTHER`**, a retired category. They are system
   Spaces created by `ensurePlatformSpaces`, never user-created, so the picker's
   classification does not really apply — but they will look like retired-category
   Spaces to any census that does not special-case them.
4. **The seed's coverage instrument counts presence, not distribution.** A state
   with one row reads the same as one with a hundred. Sufficient for "did the
   reseed lose anything", insufficient for "is this corpus realistic".
5. **`SEED_ALLOW_DESTRUCTIVE=1` still bypasses the ownership guard**, by design.
   It is now the only way to wipe a database containing a real account, which is
   the intended shape — but it is a single environment variable between a
   developer and that outcome.

---

# v2.6-SEED-2 — which absent states belong where

The §5 gaps, classified against evidence rather than instinct.

## 7. The measurement: the live account is ground truth for realism

The working database holds a real account with real Plaid data beside the demo
world. Running the same coverage instrument against it — read-only — answers
"what does a real user's database actually contain?", and the attribution is
clean because **the seed contributed zero of every state below**:

| State | live DB | on the live Space | source |
| --- | ---: | ---: | --- |
| `economicDate ≠ posting date` | 2,822 | 2,822 | ALL real Plaid |
| estimated snapshots | 736 | 736 | ALL real Plaid |
| resolved counterparty | 351 | 351 | ALL real Plaid |
| tombstoned rows | 59 | 59 | ALL real Plaid |
| events with 2+ observations | 50 | 50 | ALL real Plaid |
| distinct currencies | — | **USD only (35 accounts)** | never occurs |
| `SUMMARY_ONLY` visibility | 0 | 0 | never occurs |
| `CONTRADICTORY` settlement | 0 | 0 | never occurs (null / POSTED / PENDING) |

Proportions on the live Space:

```
economicDate ≠ posting : 2,822 / 4,053  = 70%   ← THE NORM
estimated snapshots    :   736 /   748  = 98%   ← THE NORM
2+ observation events  :    50 / 4,381  = 1.1%
```

That reframes the question. Four of these are not pathological edge cases at all
— they are **what ordinary Plaid usage produces**, and two of them are the
overwhelming majority of real rows.

## 8. Classification

### → THE SEED (their absence made it *unrealistic*, not merely thin)

| State | Why the seed | Note |
| --- | --- | --- |
| **`economicDate ≠ posting`** | 70% of real rows. A seed at 0% models a world where cards authorise and post the same day, always. | **Done — see §9** |
| **estimated snapshots** | 98% of real snapshots. | Recommended next |
| **events with 2+ observations** | The seed already creates 4 pending rows and never posts them — an *incomplete lifecycle*, unrealistic on its own terms. | Recommended next |
| **resolved counterparty** | The seed already has 39 TRANSFER rows and zero resolved counterparties. Transfers between one's own accounts are ordinary. | Recommended next |

The pattern matters: in every case **the seed already contains the precursor and
stops short of the realistic outcome**. Completing those shapes is realism, not
added complexity — which is the bar this slice was asked to hold.

### → A TARGETED FIXTURE (pathological by construction)

| State | Why not the seed |
| --- | --- |
| **a STALE event projection** (tombstoned row whose event was never re-projected) | This one cannot go in the seed *even in principle*: it is an invariant **violation**, so `audit-event-identity` would fail on every fresh seed. The state that arms an invariant is sometimes the state that breaks it. Already covered by `lib/plaid/removed-tombstone-reprojection.test.ts`, which manufactures it deliberately (v2.6-EVENT-1). |
| **`CONTRADICTORY` settlement state** | Zero in real data. It is a refusal path — a provider contradicting itself. Pathological by definition. |

⚠️ Note the distinction this forced: **a tombstoned row is realistic; a stale
projection is not.** A healthy seed would tombstone *and* re-project, producing a
correct `WITHDRAWN` event. So "tombstoned rows" is a legitimate seed candidate,
while the defect state it was originally proposed to arm belongs in a fixture.
The §5 table conflated the two.

### → INTENTIONALLY UNREPRESENTED

| State | Why leaving it out is correct |
| --- | --- |
| **multi-currency** | Zero in real data — 35 accounts, all USD. Adding a EUR account would make the demo world *less* representative of a typical user in order to arm invariants that already have unit coverage (`lib/money/`, `V25-FINAL-1`). That is precisely the trade this slice was told not to make. |
| **`SUMMARY_ONLY` visibility** | Zero in real data. The redaction path is already exercised by `BALANCE_ONLY` (8 links); a third tier adds a permutation, not a new architectural state, and the resolver has parity tests. |

## 9. The one change made

Card purchases now carry an authorisation date, exactly as Plaid returns one.

Every seeded row passed `authorizedAt: null`, so `economicDateFor` returned the
posting date and the corpus could not express a chronology divergence at all —
leaving `audit-chronology-basis` and `audit-chronology-cutover` passing
vacuously.

The lag is **deterministic** (derived from the row index, never random, so two
seeds are identical) and applies **only to card-like spending** — Dining,
Shopping, Groceries, Travel, plus every pending row, which by definition has an
authorisation and no posting. Transfers, income and fees still post without one,
because that is what happens in reality. The economic date is still produced by
the write authority; the seed states evidence and never computes the date itself.

**Result:**

```
coverage diff:  economicDate ≠ posting date   ✗ ABSENT 0  →  ✓ 105
                (nothing else changed)
18/18 REQUIRED audits pass
two consecutive seeds produce identical coverage — deterministic
```

105 of ~350 rows is 30% divergence against the live account's 70%. Lower, and
deliberately so: only genuinely card-like categories carry an authorisation.
Directionally right rather than tuned to match one user.

## 10. Verification

- 485/485 unit tests · typecheck clean · 0 lint errors
- Scratch database created, seeded twice, verified, dropped
- The working database was never seeded; the live account confirmed intact

---

## Out of scope — recorded (SEED-2)

1. **Three seed-bound states remain** (estimated snapshots, 2+ observation
   events, resolved counterparty). Each completes a shape the seed already
   starts. Not done here: one state at a time, each with its own before/after,
   is how this stayed contained.
2. **The 70% vs 30% gap is unmeasured as a realism target.** There is no stated
   standard for how closely a seed should match observed proportions, and
   inventing one to justify a number would be worse than the gap.
3. **`authorizedAt` now exists on seeded rows for the first time.** Any future
   probe that assumed it null on seeded data would silently change meaning —
   none found, but the assumption was previously safe and no longer is.

---

# v2.6-SEED-3 — closing the gap, and the one state that does not belong

The SEED-2 follow-up, measured first. Same throwaway database
(`fintracker_seedwork`), created and dropped inside this slice; the working
database was never seeded and the seed's ownership guard was re-verified
refusing it, with the real account intact afterwards.

---

## 11. Re-measurement — was the review still accurate?

Yes, with one correction. Measured on a freshly seeded scratch database at
`728cd46`:

```
states measured : 46
states PRESENT  : 42
states ABSENT   : 4
```

The four absent states were `estimated snapshots`, `events with 2+
observations`, `rows with a resolved counterparty` — **and `tombstoned rows`**,
which SEED-2's out-of-scope list counted as closed but its own §8 note had
already reclassified as "a legitimate seed candidate". Nothing else had changed:
18/18 REQUIRED audits passed, `prisma/seed.ts` and `scripts/audit-seed-coverage.ts`
were unmodified since the review, and the transfer-authority numbers reproduced
exactly.

The correction turns out not to matter, because a tombstoned row is not a
separate piece of work: it is what a settled pending row **becomes**. Doing the
multi-observation state correctly produces it.

---

## 12. Two findings the re-measurement produced that the review did not have

### 12.1 The seed had no provider identity at all

```
TransactionObservation.provider : MANUAL 350   (PLAID 0)
FinancialAccount.plaidAccountId : 0 of 24
Transaction.plaidTransactionId  : 0 of 355
ProviderAccountIdentity         : 0 rows
```

…while simultaneously creating **9 PlaidItems** and pointing **22 of 24
AccountConnections** at them. That is not a thin corpus, it is an incoherent
one: `resolveFinancialAccountId` matches an incoming Plaid transaction by
provider account id, so an account connected to an Item without one can never
receive a transaction from it.

It also meant `audit-pending-posted-desync` — REQUIRED — was **structurally**
vacuous, not merely empty. Its entire query is

```sql
JOIN "Transaction" q ON q."pendingTransactionRef" = p."plaidTransactionId"
WHERE p."plaidTransactionId" IS NOT NULL
```

which cannot return a row on a corpus where that column is null everywhere,
whatever else the corpus contains.

Live corpus, for comparison: PLAID 4,158 / MANUAL 347 observations, 10 of 35
accounts provider-sourced, each with both `plaidAccountId` and a
`ProviderAccountIdentity` row.

### 12.2 The realistic multi-observation shape is a *two-row* succession

Measured on the live account:

| shape | events |
| --- | ---: |
| successor row, linked by `providerPendingRef` | **54** |
| same row re-observed | 1 |

All 54 are POSTED, all PLAID. So "events with 2+ observations" is not an
abstract second observation — it is specifically *a pending row, a posted
successor carrying Plaid's `pending_transaction_id`, and the pending tombstoned*.
That single shape yields **three** of the absent states at once.

---

## 13. What was seeded, and what it arms

### 13.1 The pending→posted succession

Four accounts — the two checking accounts and two cards where Plaid actually
produces a settlement — now carry a provider account id (written through
`dualWriteProviderAccountIdentity`, the exchange-token route's own helper) and
every row on them a deterministic provider row id. Two of the four pending rows
are marked `settles(…, n)`; `seedTransactions` expands each into the two rows the
provider sends and replays the two syncs in production order: observe pending →
observe posted (`providerPendingRef`) → **tombstone** → **re-project**. The seed
refuses outright if the successor does not attach via `PROVIDER_PENDING_REF`.

Two pending rows are deliberately left live and unposted — the ordinary steady
state, and the left-hand side of the desync audit's join. Removing that
population would disarm the audit as thoroughly as never posting anything.

### 13.2 The transfer authority's verdict, persisted

The authority already established an account for **34 of 54** admitted legs
before this slice existed. The seed now replays
`admitTransferCandidate → resolveDestinationEvidenceFor` — the same chain
`repair-transfer-authority.ts` applies to production — and persists only where
the authority reports `persistableCounterparty`. It deliberately does **not**
apply the authority's flowType half, which on this corpus would reclassify 8
mortgage and auto-loan payments on the strength of a leg the seed does not hold.

### 13.3 The card payment's funding leg

Measured mid-slice: the seed's seven `CC Payment` rows were **one-sided** —
card-side inflows with no source of funds anywhere. A card payment is two legs
by definition, so the authority's `NO_DESTINATION_EVIDENCE` was correct, and the
consequence was that **the entire Debt Payments surface was empty on every
seeded corpus**:

```
audit-debt-payment-attestation   counted as Debt Payments : 0
audit-cashflow-debt-defect       rows the card counts: 0
                                 NAMEABLE 0 · TYPE-ONLY 0 · UNATTESTED 0
```

Seven funding legs — same amount, same day, opposite sign — close it. The
mortgage and auto-loan payments are left one-sided **on purpose**: a real Plaid
liability account frequently carries no transaction stream at all, and those 8
rows are the corpus's only instance of "a stored DEBT_PAYMENT lacking
attestation". Resolving everything would empty that branch instead.

---

## 14. Before / after

```
=== COVERAGE ===
                                          before   after
  tombstoned (soft-deleted) rows        ✗ ABSENT →  ✓  2
  events with 2+ observations           ✗ ABSENT →  ✓  2
  rows with a resolved counterparty     ✗ ABSENT →  ✓ 48
  estimated snapshots                   ✗ ABSENT →  ✗ ABSENT   (see §16)

  states PRESENT   42 → 45        states ABSENT   4 → 1
  no previously-present state was lost
```

`DEBT_PAYMENT` rows 15 → 22, observations 350 → 359, and the transfer authority
now reports the seed **idempotent** against itself: 48 persisted, 48
establishable, 48 agreeing, 0 new, 0 conflicting.

---

## 15. Verification — did the audits become capable of FAILING?

Passing is not the test. Each new state was falsified against a throwaway copy
by introducing exactly the defect the audit claims to measure.

**Un-tombstone one settled pending row** — the missed Plaid `removed[]`, the
v2.6-EVENT-1 defect:

```
audit-pending-posted-desync  exit 1  "…with a live posted successor (defect) : 1"
audit-event-reader-cutover   exit 1  ✗ multi-observation events project at most ONE live row each
audit-event-identity         exit 1  ✗ INV-4 no event has two LIVE transaction rows
```

All three passed unconditionally before, on any corpus.

**The counterparty state** produced something better than a falsification. Given
a non-empty debt card for the first time, `audit-cashflow-debt-defect`
**immediately failed on a false assertion of its own**:

```
✗ the unresolved bucket sorts LAST
```

`buildCreditorGroups` emits that bucket only when something belongs in it, and
sorts it last when it does — v2.6-OWN-2 had already corrected exactly this
mistake two checks above and missed the neighbouring line. The check demanded
that *some* row always be un-nameable, a property of one corpus rather than of
the grouping, and had never once evaluated its own claim because `groups` was
always empty. Corrected to the conditional invariant; re-verified against the
live corpus, where a bucket does exist and does sort last.

Some mutations correctly *cannot* fail these audits: re-pointing a counterparty
at a non-liability account removes the row from the debt-payment population
rather than corrupting it. That is the intended fail-closed behaviour, and it is
only observable now that the population is non-empty.

**Recorded as still vacuous, not claimed as closed:**

- `audit-transfer-identification`'s contradiction check. It needs the
  identification rung to APPLY, which needs an extracted account mask; no seeded
  account carries a `mask`, so the rung applies to **0 legs** before and after.
  Persisting counterparties did not arm it.
- `audit-transfer-authority`'s FABRICATION guard. It is asserted over the
  **authority's own verdicts** (54 of them, already non-empty), not over the
  stored column.

---

## 16. STOPPED — estimated snapshots do not belong in this seed

The one remaining state, and the one this slice refuses. Three measurements
decide it.

**It arms nothing.** `isEstimated` appears in exactly one REQUIRED audit,
`check-snapshot-integrity`, and only in its *reporting* columns
(`estimatedRows`/`observedRows`). Its failure condition — negative or non-finite
components — is entirely independent of the flag. Seeding the state would move
no REQUIRED audit from vacuous to meaningful.

**The seed cannot state it honestly.** `isEstimated` is a property of a
*computation*, defined by production as `isEstimated = tier !== "observed"`
where the tier comes from `regenerateDay`'s per-component valuation evidence.
The seed's snapshots are sine waves from `buildHistory`, produced by neither
observation nor reconstruction — and demonstrably not equal to the accounts they
claim to summarise:

```
                latest snapshot debt    accounts' actual debt
John's Space          17,229                 304,100
Smith-Doe Family      22,018                 294,000
Debt Paydown       1,915 cash / 6,466 sav        0 / 0
```

**And flipping it would refuse the demo world.** `resolveCryptoValuationState`
trusts an observation unconditionally; the moment a row is estimated with no
recorded `cryptoValuationStatus` and material crypto, it resolves to
`legacy-unrecorded` → crypto unassertable → `netWorth`, `totalAssets` and `total`
all **UNAVAILABLE**. Measured: **850 of 940** seeded snapshots carry material
crypto. Every wealth surface on Jane's, John's and the Family Space would refuse.

The only two escapes are both worse. Stamping `cryptoValuationStatus:
"supported"` asserts a supported historical price source valued that day — the
seed has no `FxRate` data and valued nothing. Stamping `"unavailable"` is honest
and produces the refusal above: a deliberately pathological corpus manufactured
to satisfy an audit that does not even read the flag.

This is the third of the three cases the brief named — **a provider/authority
contradiction**. The current value (`isEstimated=false`) is also not literally
true of a synthetic series, but it fails safe: it means FROZEN, so the
regeneration authority skips these rows rather than rewriting them and
`backfill-snapshots --rollback` never deletes them.

**The honest successor** is not "set the bit". It is: generate the seed's
snapshot history *through* `regenerateDay`/`backfillSpaceSnapshots` from the
seeded accounts and transactions, at which point `isEstimated` and
`completenessTier` are derived rather than asserted. That is blocked on the same
missing evidence — the seed has no price history — and is a separate slice with
its own measurement, because it replaces every number in the demo world.

---

## 17. Verification (SEED-3)

- Coverage: 42 → **45 of 46** states present; **no present state lost**
- **18/18 REQUIRED audits** pass on the reseeded scratch database
- Falsification: 3 REQUIRED audits **fail** for exactly the conditions they
  claim; 1 false assertion found and corrected
- Determinism: two consecutive seeds produce **identical** state counts
- 485/485 unit tests · typecheck clean · 0 lint errors
- The fixed audit re-verified **read-only against the live corpus** — still
  evaluates its real claim, still passes
- The working database was never seeded; the seed was re-verified **refusing**
  it, and the real account confirmed intact (4,536 transactions, 1,698 snapshots)
- Scratch database dropped

---

## Out of scope — recorded (SEED-3)

1. **No browser pass.** A dev server was already running against the working
   database and Next 16 refuses a second one on the same directory (and
   auto-loads `.env.local` over an injected env file), so the seeded corpus was
   never rendered. The read authorities are exercised by
   `audit-cashflow-debt-defect`, `audit-ui-truth-convergence` and
   `audit-debt-payment-attestation` against that corpus, which is the strongest
   substitute available, but it is not the same thing.
2. **`audit-transfer-identification` and `audit-transfer-authority`'s
   fabrication guard remain vacuous** (§15). Arming the first needs seeded
   account `mask` values and descriptors that carry them — a real, bounded
   follow-up that this slice measured but did not take.
3. **Provider identity covers 4 of 24 accounts.** The other 18 Plaid-connected
   demo accounts still carry a PlaidItem with no provider account id — the same
   incoherence, narrowed rather than removed.
4. **The seed now writes `ProviderAccountIdentity` rows and globally-unique
   `plaidAccountId` values** (`demo_acct_*`). Both were previously absent from
   every seeded corpus; any probe that assumed so has changed meaning.
5. **`prisma/seed.ts` now imports the transfer authority.** The seed's
   dependency surface grew from "classifier + event writer" to "classifier +
   event writer + transfer authority". Every one of them is a production
   authority and none is duplicated, but the seed is now coupled to a fourth
   subsystem's evolution.

# v2.6 REVIEW-3 Execution — Phase 0 Baseline (frozen 2026-08-17)

## Repository freeze
- Pre-program HEAD: `e61f831` (branch `v2.6`), working tree dirty (18 modified + untracked WIP).
- WIP adopted as baseline per operator instruction, committed as:
  - `96c3a8a` feat(debt): v2.6-DEBT-1 — one debt-aggregates authority with an enrolment guard
  - `0658b8e` fix(events): v2.6-EVENT-2 — provider identity outranks fingerprint adoption
  - `ecd5a76` docs: REVIEW-3 audit (program baseline)
- Program branch: `v2.6-review3`. Left untracked deliberately: `.claude/`, `docs/invoices/`, `docs/initiatives/kd15/`.

## Gates at baseline (cloud sandbox)
| Gate | Result |
|---|---|
| `tsc --noEmit` | **0 errors** |
| `eslint` | **0 errors**, 28 warnings |
| `scripts/run-tests.ts` | **455 / 489 pass.** 39 failures, all a single environmental cause: `PrismaClientInitializationError` — the sandbox cannot download the `debian-openssl-3.0.x` query engine (binaries.prisma.sh 403 via network allowlist). Zero semantic failures. Engine delivery from the dev machine pending; failures to be re-verified then. |
| REQUIRED audits (`run-audits.ts`) | Not runnable in sandbox (DB-dependent). Source-scan/enrolment guards run inside the unit suite and are green. |

## Size at baseline
1,711 TS/TSX source files · 308,420 LOC · 165 API routes · 489 test files · 105 scripts.

## Production measurements (read-only, Supabase `fourth-meridian-production`, 2026-08-17)
| Measurement | Result | Consequence |
|---|---|---|
| Space categories | OTHER×4, PERSONAL×2 — nothing else | Overview-canvas unreachability is corpus-true; retired-category deletion is corpus-safe |
| Reporting currencies | USD×6 (Spaces), USD×755 (all snapshots) | every currency finding is latent; currency fixes move no live number |
| SpaceAccountLink tiers | FULL×11 only | BALANCE_ONLY aggregation path has zero production rows; B-1 fix moves no live number |
| TransactionEvent table | **does not exist in production** (local migrations 89–100 undeployed) | event-identity/chronology semantics are freely changeable pre-deploy |
| SpaceDashboardSection | 8 user-space keys (count 2 each: net_worth, net_worth_chart, allocation, goals_progress, debt_summary, investment_summary, accounts_overview, recent_activity) + 32 platform `ops_*`/`sec_*`/`growth_*`/`cs_*` keys | platform ops dashboard is a LIVE consumer of the section table; user-financial section rows exist but render nowhere |
| SpaceGoal | 0 rows | goal divergences latent |
| PositionObservation / Holding | 294 / 9 rows | observations pipeline live in prod; Holding is residual |
| PlatformSetting registration_mode | `invite_only` | closes STATUS.md blocker #10 |
| Vercel env | INVESTMENT_OBSERVATIONS_ENABLED present (Prod), QUANTITY_AUTHORITY_MODE **absent**, PLAID_ENV present, NEXT_PUBLIC_SENTRY_DSN present; values sensitive/write-only | quantity-authority mode is default `off` in prod |

## Approvals in force
Corrections / population-convergence / chronology-convergence / display-only output movements pre-approved by the operator (2026-08-17), each to be documented with before/after in the final report. Unexplained movements halt their workstream.

# v2.6 REVIEW-3 Execution — Final Report

**Program:** one coordinated convergence-and-deletion pass closing the REVIEW-3 audit
(`v2.6-REVIEW-3-CENTRALIZATION-AND-DEAD-CODE-AUDIT.md`).
**Branch:** `v2.6-review3` (64 commits on `e61f831`). **Date:** 2026-08-17.
**Method:** 9 agents in isolated worktrees across 3 waves + an independent adversarial
falsification pass (Agent I), integrated slice-by-slice with gates at every merge.
Production grounding: read-only Supabase census + Vercel env inspection, frozen in
`v2.6-REVIEW-3-EXECUTION-BASELINE.md` **before any deletion**.

---

## 1. BEFORE / AFTER

| Metric | Before (`e61f831`+WIP) | After (`v2.6-review3`) | Δ |
|---|---|---|---|
| TS/TSX source files | 1,711 | 1,655 | **−56** |
| Source LOC | 308,420 | 298,254 | **−10,166** (net diff −6,690: +9,636/−16,326 over 336 files) |
| API routes | 165 | 163 | −2 (`platform-ops/refresh/failures`, `investments/point-detail`) |
| Scripts | 105 | 77 | **−28** (all governed: registry now covers backfill-/repair-/diagnose-) |
| Test files | 489 | 492 | +3 (guards added > tests deleted) |
| eslint | 0 errors / 28 warnings | 0 errors / **3 warnings** | −25 |
| `widget-registry.ts` | 1,107 lines | ~127 lines | −980 |
| Duplicated financial producers (audit matrix rows CONTESTED/NO-OWNER) | 19 of 42 | **3 surviving, all named** (transaction identity, wallet provider identity, goal progress — latent) | −16 |
| Undeclared money-affecting env vars | 1 (`QUANTITY_AUTHORITY_MODE`) | 0 | closed |

**Commit spine** (first-parent): WIP adoption (2) → audit+baseline docs (2) → Wave 1 merges
(slice-b population · slice-e chronology · slice-g deletions · slice-h crypto/flags) →
Wave 2 merges (slice-c AI/Brief · slice-d snapshots/currency · slice-f product surfaces) →
integration (`netWorthTrend` deletion) → Wave 3 (scripts/consolidation) → falsification
residues (`0bd6ce0`) → STATUS + this report.

## 2. FINANCIAL TRUTH MATRIX (post-program, verified by the independent falsification pass)

| Question | Calculation owner | Population owner | Chronology/window owner | Status |
|---|---|---|---|---|
| Income | `foldEconomicRow` + `attributeIncome`/`composeIncomeRollup` | `bankingTransactionWhere`+`eventProjectionWhere` | per-surface preset · `lib/time` | **SINGLE** |
| Spending | `clampEconomicSpend` (gross is a NAMED distinct measure) | same | same | **SINGLE** |
| Net cash flow | canonical net (`income − clamp`); `netAfterDebtPayments` named beside it | same | same | **SINGLE** |
| Debt total | `amountOwed` per member; aggregation site asserts it (`account-privacy.ts`) + `computeDebtAggregate` | `normalizeSharedAccounts` (product AND AI) | — | **SINGLE** |
| Debt payments | `selectDebtPaymentCashLegs` | authority's leg selection | — | **SINGLE** |
| Effective APR / terms | `resolveEffectiveDebtTerms` | — | — | **SINGLE** (provenance labels now truthful: `user`/`user_account`) |
| Utilization | `utilizationPercent`+`utilizationLevel`+`isRevolvingLine` | one membership rule, both surfaces | — | **SINGLE** |
| Liability balance semantics | `balance-semantics.ts`, breach-proof at the aggregation site | — | — | **SINGLE** |
| Net worth (now / as-of) | `classifyAccounts`→`computeSnapshotFields` (one formula) / `SpaceSnapshot` via `resolveSnapshotRowProvenance` | privacy authority / **read-boundary ratchet** | snapshot dates, `canonicalWindowChange` | **SINGLE** per basis; bases never mixed in one sentence |
| Aggregate assertability | `authoriseAggregates`, one `COMPUTED_TOLERANCE` | — | — | **SINGLE** |
| Reachable cash | `totalReachableCash` + `aggregateCurrentCashState` | privacy authority | — | **SINGLE** (hero ≡ lens) |
| Coverage months | reachable numerator + `resolveExpenseBaseline` | — | declared insufficiency when window can't grade | **SINGLE** (2 cosmetic residues noted §5) |
| Assessment verdicts | `computeAssessment` + declared `ungraded[]` | scope declared, never silent | — | **SINGLE** |
| Current/historical positions, portfolio | `getCurrentPositions` / licence path; one legacy-precedence rule (`canonical-precedence.core`) | — | evidence ceiling | **SINGLE** + declared fallback |
| Crypto valuation | native quantity canonical; USD write = documented undated-valuation presentation contract | — | dated archive for history | contract documented (migration = named future workstream) |
| Prices / FX / conversion | `prices/service` · `fx/service` · `convertMoney` (null-on-miss) | — | `yesterdayUTCISO` seam | **SINGLE** |
| Display currency | `useDisplayCurrency` + `display-money.ts`; zero hard-coded symbols in Brief/AI (guarded) | — | — | **SINGLE** |
| Chronology | — | — | **`lib/time/clock.ts` + `basis.ts`** (one clock, named bases, server-authoritative switch, source-scan guard) | **SINGLE** |
| Transaction current-state | `eventProjectionWhere` composed on list AND detail | same | event/row economic date agree **by construction** (FIRST_PENDING_OBSERVATION wired) | **SINGLE** |
| Visibility population | `grantsTransactionDetail`/`grantsAccountDetail`/`grantsBalanceDisclosure`, fail-closed | — | — | **SINGLE** |
| Cross-Space totals | deliberately NO owner — deterministic deduped figure + never-add rule in the prompt, failed Spaces named | — | — | declared & disclosed |
| **Surviving exceptions** | transaction identity (3 keys: event-evidence/fingerprint/resolver — out of program scope, see §5) · wallet identity (address vs PAI) · goal progress (multi-impl, latent: 0 production rows, feature itself awaiting a product decision) | | | |

## 3. OUTPUT CHANGES (all pre-approved corrections; production impact measured against the census)

Production is all-USD, all-FULL links, 0 goals — so **most corrections move no live number today**. Definitional changes:

| Figure | Before | After | Class | Live impact |
|---|---|---|---|---|
| AI/Brief `netCashFlow` | `income+refunds−gross−debtPayments` | canonical `income−clamp(gross−refunds)`; old value ≈ new `netAfterDebtPayments` | correction | Brief deficit verdict can no longer contradict the workspace |
| AI `debtPaymentTotal` | sign proxy (`isDebtPayment && amt<0`) | authority cash legs (attested; liability legs refused) | correction | corpus-dependent (the audit's $239K/$245K class) |
| AI `incomeTotal`/classes | NOT_INCOME gate dead (non-column read) | real `attributeIncome` derivation; NOT_INCOME excluded & disclosed | correction | e.g. the documented $495.65 issuer credits leave income |
| Brief savings rate | route arithmetic over gross | canonical net; gross named as gross, refunds disclosed | correction | differs only when refunds in window |
| Brief net-worth sentence | snapshot delta beside live "now" | single population (canonical window endpoints) | population convergence | consent-gated-investment mismatch impossible |
| Brief/AI money strings | hard-coded `$` | reporting-currency threaded; guard bans symbols | display-only | none (all-USD) |
| Launcher net worth | raw latest snapshot | most recent **admissible** point (authorisation+FX), ≈ marker on estimated | correction | byte-identical for every current production row (all 755 assertable/USD) |
| Debt/BALANCE_ONLY aggregates | raw signed sum (credit nets debt) | Σ amountOwed + disclosed creditTotal + member-resolved reachable | correction | latent (0 BALANCE_ONLY links) — guarded by an aggregation-site throw |
| Space utilization population | any `creditLimit>0` | canonical revolving-line rule (matches Credit page) | population convergence | latent unless a loan carries a limit |
| Signal/Brief trend | accidental fetched-row window, no sign check | `canonicalChange`, sign-correct verbs | correction | closes the recorded 14.9%-vs-47.4% and "up −3.2%" classes |
| Accounts tab names | raw provider `name` | `accountDisplayName` (full evidence select) | correction | renamed accounts finally display renamed |

## 4. DEAD CODE REMOVED (proof per commit; classes per the audit's scheme)

- **Product surface (B):** OverviewWorkspace + summary branch, SpaceTrendHero, PerspectiveSwitcher, SpaceComingSoonPanel, PerspectivesWidget, SpaceSectionStack, RecentTransactionsPanel, RebuildHistoryButton (capability preserved, §5), NetWorthChart family (4 files), hero rendering path, 22+ SectionRegistry entries, 4 perspective `widgets[]` arrays, widget-registry cut to consumed truth, presets cut to `goals_progress`, dead nav ids (PERSPECTIVES/FINANCES/DOCUMENTS/placeholder machinery), legacy tab mapping, category-write bypasses **fixed** (allowlist PERSONAL/FAMILY/CUSTOM/OTHER).
- **Definitely dead (A):** providers/catalog, ownership-windows, historical-probes, net-worth-point-detail (+ guard repointed to the live authority), debt-payments tombstone (+ guard repointed), atlas/EmptyState, marketing FeatureGrid/Hero (+ dead copy exports), LiquidityLadderTiers, six retired Brief components, brief-types map/basis surface, refresh/failures route (+ FailureSummary core/types), investments/point-detail route + historical-point-detail authority, getHoldings, stall-projection (superseded by incidents lifecycle), serializeSpaceConversionContext, FxUnavailableNote, rebuild-range, cash-flow-adapters (post-F2 orphan), zero-import helpers across balances/snapshots/flow-predicates/income-source, 16 dead env accessors, `netWorthTrend` pair, dead Brief branches (DEBT case honest-reachable now; GOAL_COMPLETED genuinely reachable now).
- **Migration/incident tooling (C):** 26 scripts deleted (14 one-shot backfills, 9 dated forensics [2 event-repair scripts RETAINED until the event migration deploys], 5 registry-retired), + their script-only lib modules; **governance extended** so unclassified backfill-/repair-/diagnose- scripts fail CI; 9 operator tools made discoverable via npm scripts.
- **Duplicates after convergence (D):** the hand-copied snapshot formula (now imports `computeSnapshotFields`), the format.ts currency trio (single implementation + regrowth guard), tolerance duplication (one constant), the AI's three economic folds, local threshold copies, four inline FULL checks, three "now" implementations.

## 5. LEFT IN PLACE — evidence insufficient or product decision required

**Product decisions (the unresolved bucket):**
1. **Goals/Retirement have no reachable entry point** (deep-link only; 0 production goals). Re-add an entry (rail/lens) or retire the feature — retiring releases RoutedWorkspaceModal, GoalsCard, AddGoalModal, goal adapters + keys, and the goal-progress multi-implementation (the last latent MULTI in the matrix).
2. **Snapshot amendment is API-only** — the capability (`snapshot-amendment.ts`, `POST /wealth/amend`, tables with production rows) is a valid recovery path whose only UI died with the Overview canvas. Needs an operator entry.
3. **HOUSEHOLD** is fully unwritable (excluded from the allowlist by evidence: hidden template "merged into Family", zero prod rows). Re-admit deliberately or schedule enum retirement (with GOAL/TRIP/VEHICLE/EQUIPMENT/DEBT_PAYOFF/EMERGENCY_FUND/INVESTMENT/BUSINESS/PROPERTY — enum members kept; Postgres enum drops are their own program).
4. **Brief debt-grading scope:** the Brief now *declares* the withheld debt grade; carrying the per-account list under brief scope would make debt gradeable there at payload cost.
5. **Crypto valuation spine completion** (named workstream): unify the three BTC→USD paths onto one dated valuation authority + provenance column (schema); retire `writeBtcHolding`/`sync-current-holdings` (P2-6 close-out). Until then the documented contract governs.
6. **SpaceSnapshot partial-sum disclosure columns** (named schema follow-up from slice D): `completenessTier:"incomplete"` now records the fact; which component was partial needs a migration window.
7. **Transaction identity's three keys** (event-evidence vs fingerprint vs resolver) — real, pre-existing, out of this program's scope; the detail-drawer "duplicate" claim still uses the enriched-merchant key the doctrine rejects.
8. **Dormant-valid kept:** legacy-crypto-holdings (deletion condition unmet, now with one precedence rule), capability-classification (deliberate intent record, no-import guard), SUMMARY_ONLY/PRIVATE enum values (readers fail closed), event repair scripts (until migrations 89-100 deploy), CRYPTO_LEDGER_POPULATION, the four CRON-guarded job revert routes, `app/prototype/*` + containment guard.
9. **Cosmetic residues, recorded:** `LiquidityHero` local 6/3 color bands; latent `emergency_fund_progress` renderer computing coverage from raw config (unseeded, 0 rows); `DebtPayoffSection` raw-signed sort; `<img>`/react-hooks lint advisories (3).

## 6. CURRENT PRODUCT SHAPE

- **Creatable Spaces:** FAMILY, CUSTOM (live templates), OTHER (legacy fallback), PERSONAL (registration). Category writes validated everywhere.
- **Reachable Space surfaces:** OVERVIEW always renders the engaged lens (Wealth default; Net Worth/CashFlow/Liquidity/Investments/Debt via WORKSPACE_RENDERERS) · TRANSACTIONS · ACCOUNTS (AccountsLedger, display-identity-correct) · ACTIVITY · MEMBERS. GOALS/RETIREMENT: deep-link-only (decision pending).
- **Sections:** user-financial DB sections render only via the GOALS/RETIREMENT modals (`goals_progress` is the sole seeded key); **the platform ops dashboard's 32 section rows are untouched and live** (separate registry, verified).
- **Brief/AI:** one context builder; assemblers enrolled on canonical authorities; assessment with declared insufficiency; master chat with deterministic dedupe + failure disclosure; every money string currency-threaded; validators/guards: fold-enrolment, currency-presentation, clock-authority, snapshot-read-boundary, display-currency-fallback, aggregation-site invariant.
- **Providers:** Plaid via PAI-first reconcile; BTC wallet native-canonical with documented USD-presentation contract; prices/FX single-owner archives; all flags declared.

## 7. GATES

| Gate | Result |
|---|---|
| `tsc --noEmit` | **0 errors** (clean after every slice merge) |
| `eslint` | **0 errors, 3 warnings** (28 at baseline; survivors pre-existing advisories) |
| Unit suite (sandbox) | **452/492 pass; 40 failures, every one `PrismaClientInitializationError`** — the sandbox cannot fetch the Linux Prisma engine (network allowlist). Verified environmental at baseline and at every merge; count wobbles ±1 by a documented async crash race, always the same class. |
| **Full suite with engine** | **PENDING — the program is NOT declared green until this runs.** Either drop `libquery_engine-debian.so.node.gz` into `_to_delete/` (cloud verification) or run `npm run test:unit` on the delivered branch on macOS (darwin engine present). |
| REQUIRED audits | DB-dependent; not runnable in the sandbox. Source-scan/enrolment guards (superset relevant to this program) run inside the unit suite: green. Run `npm run audit` locally after checkout. |
| Financial fingerprints | Every definitional movement enumerated in §3 with class + reason; pre-approved 2026-08-17. No unexplained movement encountered; no workstream halted. |
| Production mutation | None. Phase-0 census was read-only SELECTs + dashboard reads. |

## 8. FINAL VERDICT

1. **Is Semantic Authority Convergence complete now?** **Yes, at program scope — with three named, latent exceptions** (transaction-identity keys, wallet identity, goal progress) and the §5 decision list. Every question in the matrix has a calculation owner; population and window ownership — the audit's actual gap — now exist as *code with guards* (privacy authority, read-boundary ratchet, clock seam, fold enrolment), not as convention.
2. **Can two current product surfaces answer the same financial question differently?** **Not through any path this program could construct or the falsification pass could reproduce.** All 12 recorded contradictions verified non-reproducible; the guards are now at the *sites* (aggregation throw, source-scan ratchets with accurate allowlists), not downstream.
3. **Is there any known current-product dead code left?** No **known** class-A dead code. Known-and-kept: the §5 dormant-valid list, each with a written reason.
4. **What remains intentionally unresolved?** §5 items 1–7.
5. **Is the foundation safe for Context Frame → Attention → Decision Engine?** The three blockers the audit named are closed: chronology has one owner; population has one owner; the attention inputs (assessment + signals) now share vocabulary and declare insufficiency instead of going silent. **Yes — with the §5 decisions made first** (goals entry, Brief debt scope), since Attention will surface both.
6. **Cost of the program:** −10,166 LOC, −28 scripts, −2 routes, −25 lint warnings, +the guard set. Fewer paths, stronger boundaries, one answer per financial question.

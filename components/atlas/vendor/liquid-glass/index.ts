/**
 * Vendored Liquid Glass — Card only. See VENDORED.md for provenance/attribution.
 * Only the Card component + the settings types are re-exported; the WebGL engine
 * (core/) is an implementation detail.
 */
export { LiquidGlassCard, type LiquidGlassCardProps } from "./LiquidGlassCard";
export type { LiquidGlassSettings, LiquidGlassVariant } from "./core/types";

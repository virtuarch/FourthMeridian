"use client";

/**
 * BriefActions — retired
 *
 * Primary CTAs ("Continue to Dashboard", "View AI Analysis") were moved
 * directly into BriefHero so users can act without scrolling past the cards.
 *
 * This component is kept as a no-op to avoid breaking any future imports,
 * but it renders nothing. It can be deleted once all references are removed.
 *
 * TODO: delete this file and remove its import from DailyBriefClient.tsx
 */

export function BriefActions() {
  return null;
}

# Fourth Meridian — STATUS

**This is the only file allowed to describe the current state of the project.**
Every other document is either an immutable decision record or an operational reference.
If this file conflicts with any other document, this file wins. If it conflicts with the code, fix this file.

| | |
|---|---|
| Last verified | 2026-07-06, against the working tree post-`f22de52` (MC1 complete incl. **QA Q1–Q6**; FX historical archive backfill applied; MC1 performance **P0** batched FX reads; navigation currency-staleness fixed; FlowType Desync **certified**; **TI Phase 1** complete — all landed since the `f22de52` checkpoint) |
| Maintenance rule | Any PR that changes system behavior updates this file, or states in the PR why not |
| Supersedes | `ROADMAP.md`, `docs/operations/PROJECT_STATE.md`, and the status tables in `docs/architecture/PHASE_2_CANONICAL_ROADMAP_AUDIT.md` |

## Verification

Last verified against:

- Commit: `f22de52` (+ subsequent working-tree changes)
- Date: 2026-07-06

Related investigations:

- V2.5 Architecture Status Audit (2026-07-04) — `docs/investigations/V2.5_ARCHITECTURE_STATUS_AUDIT_2026-07-04.md`
- STATUS Drift Correction (2026-07-04) — `docs/investigations/STATUS_DRIFT_CORRECTION_2026-07-04.md`
- Physical Schema Cleanup — Workspace → Space physical DB rename (`DB1`) — **implemented & applied 2026-07-05** (migration `20260705134641_db1_rename_workspace_physical_to_space`; Prisma "Already in sync", tsc + lint green). Follow-up **LC1 — Workspace Retirement Cleanup** removed residual `WorkspaceAccountShare`-era dead code/comments. Records: §3 (DB-x track), §5 (DB1 block), `docs/initiatives/db1/DB1_IMPLEMENTATION_CHECKLIST.md`.
- **D-TEST** (`fcadc2a`) — unified test runner + CI foundation: `npm test` → `scripts/run-tests.ts`, CI at `.github/workflows/ci.yml`. Closes the "no test runner" caveat in §5 v2.4.5 / §6 blocker 6 (the *missing suites* named there stay open).
- **Merchant Intelligence foundation Slice 1** (`275a9c8`, `c8601b9`) — deterministic merchant rules + seed (categorization layer only; upstream of the flow classifier, outputs categories, never flow).
- **FlowType P5 closeout** (2026-07-05) — read cutover Slices 0–7 complete; single semantic authority achieved. Closeout investigation: `docs/initiatives/flowtype/P5_CLOSEOUT_INVESTIGATION_2026-07-05.md`; next-initiative record: `docs/investigations/NEXT_INITIATIVE_AND_ROUTER_E668_INVESTIGATION_2026-07-05.md`. See the FlowType ledger entry in §3.
- **MC1 QA (Q1–Q6)** complete 2026-07-06 — post-closeout currency-propagation cleanup across Space surfaces (mislabeled conversions, un-threaded aggregates, itemized-row currencies, the SpaceDashboard family, per-Space Space-card currency incl. the Q5b off-stamp-conversion fix, and the Q6 currency-switch live-update). Investigations: `docs/initiatives/mc1/MC1_QA_Q4B_INVESTIGATION_2026-07-06.md`, `docs/initiatives/mc1/MC1_QA_CURRENCY_PROPAGATION_AUDIT_2026-07-05.md`.
- **FX historical archive backfill** applied 2026-07-06 — `scripts/backfill-fx-rates.ts` run from the earliest `SpaceSnapshot` date, so pre-cron history now converts (closes the OPS step named in the Q4b investigation).
- **MC1 performance P0** (2026-07-06) — batched the FX archive prefetch in `buildConversionContext` (one range read per window replaces N sequential per-date reads); conversions byte-identical, proven by `lib/money/context-batch.test.ts`. Investigation: `docs/initiatives/mc1/MC1_QA_PERFORMANCE_INVESTIGATION_2026-07-06.md`.
- **MC1 navigation currency-staleness** (2026-07-06) — investigated **and fixed**: page-scoped `DisplayCurrencyProvider` from the freshly resolved Space context, `router.refresh()` parity on the Spaces-grid switch, and `spaceId` host keying, so a Space switch no longer carries the previous Space's currency until a manual refresh. Investigation: `docs/initiatives/mc1/MC1_NAV_CURRENCY_STALENESS_INVESTIGATION_2026-07-06.md`.
- **Spaces shell unification** (2026-07-08) — **complete**. Personal now renders through the shared `SpaceDashboard` shell (SP-2A-4c flip) with `PersonalHero` injected via the `renderHero` seam, and the legacy `DashboardClient` is **retired (deleted)** (SP-2A-4d). One dashboard system now serves Personal and shared Spaces. Investigation: `docs/initiatives/d9/SP-2A-4_PERSONAL_SHELL_SWAP_INVESTIGATION_2026-07-08.md`. See §3 (SP-x foundation table) and §5 (v2.5).
- **Cash Flow Perspective redesign** (2026-07-12, `4265033` S1–S3 · `85a7539` S4) — presentation-only recompose of the Cash Flow Perspective from the generic single-column `SectionCard` stack into a bespoke multi-panel grid (`components/space/widgets/cashflow/CashFlowPerspective.tsx`), mirroring the landed `WealthPerspective` composition pattern — no widget registry / schema / grid engine, no new time model. The same five widgets (Summary · History · Spending by Category · Debt Payments · Income by Source) are relocated unchanged behind one bounded `activePerspectiveId === "cashFlow"` branch in `SpaceDashboard.tsx`; **calendar usability is byte-identical** (`CashFlowCalendar`/`CashFlowHistoryWidget` untouched — the History widget receives identical props). S4 added a **Key Insights** panel — the first consumer of the previously-orphaned `lib/transactions/cash-flow-compare.ts` (deterministic then-vs-now / top-category / top-source / credit / caveat bullets, `buildCashFlowInsights` + `previousEquivalentPeriod`) — and made the shell **Completeness chip dynamic** for Cash Flow via a host-memoized `cashFlowStamp` threaded through `resolvePerspectiveEnvelope` (absent ⇒ unchanged static boundary). Tests: `CashFlowPerspective.test.ts`, `cash-flow-insights.test.ts`, extended `envelope.test.ts` (suite green, tsc + eslint clean). Plan: `FOURTH_MERIDIAN_CASHFLOW_PERSPECTIVE_REDESIGN_IMPLEMENTATION_PLAN_2026-07-12.md`.

---

## Current focus / Next initiative

**Recently completed (2026-07-06):** MC1 implementation (all phases) **and** MC1 QA (**Q1–Q6**) are complete; the **FX historical archive backfill** is applied; **MC1 performance P0** (batched FX archive reads) landed; the **navigation currency-staleness** issue was investigated and fixed. The FlowType **Desync** initiative is **certified** — `npm run audit:flow-desync` passes, **no database remediation was required**, and the corpus is certified clean (see the FlowType entry in §3). **Transaction Intelligence (TI) Phase 1 is complete.**

**Also completed (2026-07-08): Spaces shell unification.** Personal is now a **first-class Space rendered through `SpaceDashboard`**; the legacy `DashboardClient` is **retired (deleted)**. One dashboard system now serves Personal and shared Spaces, on the settled doctrine **Space → Template → Materialized Sections → Widgets**, with the **Hero** (`PersonalHero`, injected via the `renderHero` seam) as the **only sanctioned per-Space divergence**. See §3 (SP-x foundation table) and §5 (v2.5). The Spaces architecture is complete.

**Current initiative:** **OPS-1 — Platform Operations Foundation.**

**Upcoming (execution order):** OPS-1 S0/S1 → **PO1 — Event Grammar & Telemetry** → Platform Facts → Platform Rollups → Platform Operations → Private Beta.

**Parallel product track (opened 2026-07-07):** **MI1 — Merchant Intelligence** — **M0–M6 complete (2026-07-07)**; ran as a bounded, schema-owning product track alongside the OPS platform lane. Post-M6, the merchant identity doctrine and the merge-suggestions/review-queue investigation are recorded in `docs/initiatives/mi1/`. Successor: **MI2 — Merchant Merge Review (Planned)** — sanctioned merge core + CLI (S1), then a human-confirmed review queue (S2); no auto-merges. See §3 (Other) and `docs/initiatives/mi1/`.

**Recommended next workstream (product lane): MI2 S1 — Merchant Merge Core & CLI.** With the **TI build phase complete**, the **Spaces architecture complete**, and the **MI1 investigations already written**, the next product-lane step is to turn the one-off `scripts/merge-wgu-merchants.ts` into a reusable merge core (`lib/transactions/merchant-merge.ts`) plus a **dry-run-default** `merge-merchants` CLI — a single authoritative merge contract as the only sanctioned execution path. **No UI, no schema, no auto-merge** at this slice. The next implementation prompt should be **MI2 S1 investigation / implementation planning — not Spaces** (Spaces implementation is closed; do not reopen). See MI2 in §3 (Other).

**Platform lane unchanged:** **OPS-1 → PO1 — Event Grammar & Telemetry** remains the platform track and is **not** the immediate product-lane next step. **Near-term follow-ups (deferred, not next):** **SP-3 template provenance** (deferred); **drag/drop section customization** — now *enabled* by the standardized materialized-section model but intentionally not next; **UX2 Plaid initial-sync / freshness** — deferred until after the MI2 foundation.

---

## 1. Project overview

Fourth Meridian is a personal finance platform built on Next.js 16 / Prisma / PostgreSQL, deployed to Vercel (sin1) with Supabase Postgres.

What exists and works today:

- **Accounts & sync** — Plaid Link, cursor-based incremental transaction sync with retry and manual-refresh cooldown, automatic duplicate-account reconciliation that survives Plaid reissuing account/transaction IDs, crypto wallet tracking by address, CSV import (MVP), soft-delete lifecycle throughout.
- **Spaces** — multi-tenant financial contexts (personal / shared) with roles (OWNER/ADMIN/MEMBER/VIEWER), invites, goals (4 types), per-Space dashboards, and graduated account sharing (FULL / BALANCE_ONLY / SUMMARY_ONLY) with read-time redaction.
- **AI analyst** — Space-scoped chat: deterministic intent routing, dynamic transaction windows with follow-up carry-forward, deterministic monthly rollups / merchant normalization / spending trends, a deterministic financial assessment engine (data quality, cash flow, debt, liquidity, capital allocation, risk/opportunity), knowledge-gap capture, provenance-first prompting. LLM: OpenAI gpt-4o-mini behind a single provider boundary.
- **Security** — AES-256-GCM with HKDF per-purpose key derivation, hashed password-reset tokens, TOTP + recovery codes, session management with revocation cache, append-only audit log, SYSTEM_ADMIN role with a fully separate admin surface and redacted provider diagnostics.

What does **not** exist yet: automated background jobs beyond one daily bank-sync cron (scheduler entrypoint never invoked; AI advice job is a stub), broad test coverage (output validator, privacy suites, intent classifier + characterization suite), billing, production Plaid credentials. (Live output validation on LLM replies now exists — the AI-4 validator is enforcing, not shadow — see KD-2; rate limiting is implemented but off by default — see KD-3.)

## 2. Current version

| | |
|---|---|
| Architecture phase | **v2.5 — Spaces Completion + Design Foundation — IN PROGRESS.** v2.4/v2.4.5 merged and tagged; foundation closed (see §5) |
| package.json | `2.4.5` |
| Latest tag | `v2.4.5` (merge of `feature/phase-2-architecture`, commit `6b517fa`) |
| Active branch | `feature/v2.5-spaces-completion` (working tree clean as of the FlowType P5 closeout commit; the Liquid card redesign is committed, see §3 UI-1) |
| Baseline of record | `v2.4.5` |

## 3. Initiative ledger

Statuses: **Complete · Active · Planned · Parked · Deprecated**

### Decision Matrix initiatives (D1–D14 — IDs frozen, defined only by `PHASE_2_DECISION_MATRIX.md`)

| ID | Name | Status | Description | Evidence | Next milestone |
|---|---|---|---|---|---|
| D1 | DuplicateAccountCandidate audit behavior | Complete | Repurposed as append-only ledger of automatic merges | `lib/accounts/reconcile.ts`; commit `94aa6e2` | — |
| D2 | Provider & Connection architecture | Active | Production hardening (7A–7G) complete; residuals open: import pipeline remainder (4D), adapter generalization (5), first sync-provider selection (6), stabilization incl. PLAID-fallback removal (7) | `docs/initiatives/d2/D2_STEP7G_...CLOSEOUT_AUDIT.md` | Apply 5 doc-only fixes from `V24_COMPLETION_PLANNING_INVESTIGATION.md` §1, then move residuals to this ledger and freeze `D2_ROADMAP.md` |
| D3 | WorkspaceAccountShare → SpaceAccountLink | Complete | **SpaceAccountLink is the canonical — and sole runtime — account-sharing model; WorkspaceAccountShare is retired** (v2.5-A Phase 4c, 2026-07-03, `e17c699`): model + all 4 back-relations dropped (migration `20260703120000_v25a_retire_workspace_account_share`), zero runtime references remain, seed rewritten to FinancialAccount + AccountConnection + SpaceAccountLink, legacy WAS backfill/verification scripts retired. Phase 0 seam gates final: **A PASS · B PASS · C PASS · D RETIRED · E PASS** (`scripts/phase0-seam-gates.ts`). Local validation all green: `prisma migrate dev` · `prisma generate` · `tsc --noEmit` · `eslint` · `prisma db seed` · KD-1/KD-15/KD-19 two-user privacy proof · Phase 0 seam gates. Deliberately retained pending separate approval: legacy `Account`, `Holding.accountId`, `Transaction.accountId`, `VisibilityLevel.SHARED` — legacy `Account` retirement is a separate future milestone, not part of this closure | `prisma/migrations/20260703120000_v25a_retire_workspace_account_share/migration.sql`; `docs/investigations/V25A_PHASE0_SEAM_RETIREMENT_READINESS.md` | — (legacy `Account` retirement is tracked separately, gated on prod Phase 0 counts + explicit approval) |
| D4 | AI Context Builder enforcement + agentScope | Active | `agentScope` shipped and intersected in builder; runtime membership guard live; lint-rule enforcement of the no-decrypt boundary not verified. **AiAgent auto-creation gap closed** (`37f96f3`): Spaces could exist without an `AiAgent` row — the `spaces`/`brief` routes and the seed now create the missing agent | `lib/ai/context-builder.ts`; `AiAgent.agentScope` migration; `37f96f3` (`app/api/spaces/route.ts`, `app/api/brief/route.ts`, `prisma/seed.ts`) | Verify/add lint enforcement (v2.4.5) |
| D5 | Job scheduler entrypoint + missing jobs | Active (reopened) | Daily bank-sync Vercel cron works; `startScheduler()` never invoked; `run-ai-advice.ts` and `sync-crypto.ts` are stubs. Audit's "Complete" status was premature | `vercel.json`; `jobs/scheduler.ts` header; `jobs/run-ai-advice.ts` | Scheduler substrate + AiAdvice write path (v2.6b entry criteria) |
| D6 | ProviderCatalog field reconciliation | Active | Slice 1 static catalog module shipped; reconciliation decision not fully executed | `lib/providers/catalog.ts`; `docs/initiatives/d6/` | Resume when a second provider or launch UI needs it |
| D7 | ProviderCatalog admin CRUD | Planned | Minimal SYSTEM_ADMIN-gated CRUD | Decision Matrix §D7 | After D6 completes |
| D8 | Lifecycle consistency rule | Complete | Soft-delete by default; archive tier only where UX needs undo | Decision Matrix §D8; applied to all Phase 2 tables | Known deviation: Plaid `removed` hard-deletes (defect KD-9) |
| D9 | SpaceTemplate / marketplace foundation | Parked | See §8 | Decision Matrix §D9 | — |
| D10 | Deferred-scope ratification | Complete | Marketplace/billing/messaging/support ratified out of Phase 2 | Decision Matrix §D10 | Billing ban lifts at v3.0, nowhere earlier |
| D11 | Schema modernization | Complete | Holding→FinancialAccount FK, hashed reset tokens, `createdByUserId` | Migrations `20260622150000`, `20260622...`; `lib/password-reset-token.ts` | — |
| D12 | Internal-ops Spaces / `isInternal` | Parked | See §8 | Decision Matrix §D12 | — |
| D13 | Connection.credential nullability | Complete | Nullable as decided | `prisma/schema.prisma` Connection model | — |
| D14 | Encryption key derivation (HKDF) | Complete | Per-purpose subkeys, dual-format reads. Audit's "Not Started" status was wrong (implementation lives in `lib/plaid/encryption.ts`, not `lib/encryption.ts`) | `lib/plaid/encryption.ts` | v1→v2 re-encryption (KD-6/SEC-1) code-complete; only the v1 read-branch removal gate remains |

### D2-derived orchestration initiatives (not frozen matrix IDs)

`D2.x` is a derivative-of-D2 label, **not** a new frozen matrix integer (D1–D14 stay frozen per §4) and not an `AI-x`/`UI-x`/`L-x` slot — it denotes an orchestration/UX layer built on the D2 sync engine. Folder allocated at `docs/initiatives/d2x/` so the ID cannot be squatted (§4 namespace rule).

| ID | Name | Status | Description | Evidence | Next milestone |
|---|---|---|---|---|---|
| D2.x | Initial Sync Experience & Historical Pipeline | **✅ Complete** (2026-07-05) · ⭐ v2.5 flagship UX | Orchestration + UX + data-integrity foundation. **All in-scope slices shipped:** Slice 1 fast-path split (balances immediate; `deferHistorySync` + `historyPending`) → Slice 2 background history via `after()` (reusing `syncTransactionsForItem`) → Slice 3 sync-status endpoint (`GET /api/sync/status`, state from `PlaidItem.cursor`) + permanent `/dashboard/connections` hub (canonical Liquid `ConnectionCard`, provider-aware, Connect-institution + Add-wallet actions) → Slice 4 30-day `SpaceSnapshot` backfill (additive `isEstimated`, chart badge) + Slice 4B credit-card debt reconstruction → snapshot orchestration fix (all-items refresh snapshots each Space once after all institutions, excludes failed-item Spaces) → transaction-sync integrity hardening (`removed[]` soft-delete + resurrection; recovery script) → Data Integrity Gate M1 (`SyncIssue` table) + M2 (balance↔transaction reconciliation, flag-only). Migrations applied: `d2x_slice4_snapshot_isestimated`, `d2x_sync_issue_integrity_gate`. Progressive-reveal contract is in place for whatever surfaces exist (Daily Brief generation → v2.6b; richer AI → AI-x — on their own tracks). **Deferred (non-blockers):** M2 auto-replay → v2.5; SyncJob / Plaid `SYNC_UPDATES_AVAILABLE` webhook / retry hardening → v2.5; snapshot `quality` column, AI/Brief sync-health consumption, `pending_transaction_id` reconciliation, fingerprint over-match hardening → v2.6b. Deeper-than-30-day history → Snapshot Backfill initiative. | `docs/initiatives/d2x/D2X_CLOSEOUT_AUDIT.md`; `docs/initiatives/d2x/D2X_INITIAL_SYNC_EXPERIENCE_CHARTER.md`; `docs/initiatives/d2x/D2X_DATA_INTEGRITY_GATE_FINAL_CHECKLIST.md` | ✅ Complete — deferrals tracked under v2.5 / v2.6b |

### AI intelligence track (AI-x — see §4 for aliasing)

| ID | Name | Status | Description | Evidence | Next milestone |
|---|---|---|---|---|---|
| AI-1 | Context Builder + domain assemblers | Complete | `buildContext()`, assembler/signal registries, accounts/transactions/goals/snapshot/holdings assemblers | `lib/ai/context-builder.ts`, `lib/ai/assemblers/` | — |
| AI-2 | Deterministic assessment engine | Complete | Data quality, cash flow, debt, liquidity, allocation, debt strategy, spending, trends, goal alignment, readiness, risk/opportunity | `lib/ai/intelligence/annotations.ts` | — |
| AI-3 | Intelligence expansion (formerly "D6.3") | Complete (pending stabilization) | Dynamic windows, carry-forward, ambiguity guard, drilldown, monthly rollups, merchant/income rollups, trends, shadow planner | `app/api/ai/chat/route.ts`; `lib/ai/context-priority/` | Closes only when AI-4 items in v2.4.5 land |
| AI-4 | Verification layer | Active | Output validator promoted from shadow to **live enforcement** (`27fb872`, KD-2 closed): pure membership-based number reconciliation now drives `applyEnforcement` at the chat choke point — default `annotate` appends a caveat to any reply with an unreconciled figure; `AI_OUTPUT_VALIDATION_MODE` supports `shadow`/`annotate`/`block`; audit logging preserved; fails open. Intent heuristics consolidated into tested modules (KD-11). Remaining: planner promoted from shadow to live budgeting | `lib/ai/output-validator.ts`; `docs/initiatives/ai4/AI-4_PHASE_0_INVESTIGATION.md`; `docs/initiatives/ai4/KD-2_LIVE_ENFORCEMENT_INVESTIGATION.md`; PE review (2026-07-02) | v2.6b (planner live) |
| AI-5 | Advisor Intelligence | Planned | Deterministic conversation-state layer above the hardened pipeline (no LLM above the validator): conversation-state substrate; active-window + context-change disclosure; confidence/completeness propagation; intent-path consistency (KD-16); graceful context compression; advisor-quality presentation; follow-up/goal reasoning (stretch). Approved 2026-07-02 | Charter: `docs/initiatives/ai5/AI-5_ADVISOR_INTELLIGENCE_PROPOSAL.md`; 2026-07 real-world testing session | v2.6a (owns the milestone); gates v2.6b |

### Multi-currency architecture track (MC-x — see §4 for the namespace prefix)

`MC-x` is a new track prefix (multi-currency / money-model), allocated per the §4 namespace rule with a folder at `docs/initiatives/mc1/` so the ID cannot be squatted. **Foundational architecture track** — future provider integrations depend on it and should build on it rather than precede it.

| ID | Name | Status | Description | Evidence | Next milestone |
|---|---|---|---|---|---|
| MC1 | Multi-Currency Architecture | **✅ COMPLETE — ALL PHASES (2026-07-05)**: Phase 0 provenance `298ef56`→`bf53507` · Phase 1 FX layer `8689e2d`→`b0adb72` (live-validated vs OXR) · Phase 2 read-time conversion `030dc07`→`b234e3e` · Phase 3 the flip `257f63f`→`ce51382` · Phase 4 selector & UX (display plumbing, Space/User selectors, "≈ est." presentation, mixed-stamp charts, holdings, panel API, AI label+disclosure, ephemeral override). Zero stored-fact mutation across 19 slices; final closeout + residual ledger: `docs/initiatives/mc1/MC1_FINAL_CLOSEOUT_REPORT_2026-07-05.md` · 🏛️ foundational architecture | Evolve Fourth Meridian from USD-first into a first-class multi-currency system **without future rewrites**. Approved direction (from investigation): **Phase 0 provenance stamping approved** (USD-era, additive, behavior-neutral) · **Option B** storage model (original + normalized values, frozen at write time) · **reporting currency on the Space** (per-Space currencies allowed; chart/AI overrides ephemeral only) · **row-level currency** on transactions/holdings/snapshots, not only accounts · **historical FX** for history, never today's rate · **crypto modeled as an asset with fiat valuation**, not a cash currency. The aggregation chokepoint is `sumBalances()`/`classifyAccounts()` — the conversion cutover isolates there. **Provenance is separated from conversion and sequenced first because it cannot be reconstructed later.** Interactive Brokers (multi-currency positions inside one account) validates the row-level-currency requirement. **Not a v2.5 deliverable** — follows D2.x + Snapshot Backfill, precedes future provider expansion (Coinbase, Kraken, IBKR, Schwab, Fidelity, CSV, wallets). Phased outline (**restructured & approved 2026-07-05** — `docs/initiatives/mc1/MC1_MULTI_CURRENCY_ROADMAP.md`, supersedes the original 0–7 outline): **0** currency provenance (provenance only — `Transaction.currency`, `Holding.currency`, `SpaceSnapshot.reportingCurrency`, writer stamping, backfill; behavior-neutral) → **1** FX provider layer (immutable dated rate archive + deterministic service) → **2** read-time conversion via shared money service (*recorded revision: read-time conversion preferred over Option B write-time normalized columns for now; conversion never mutates stored financial facts; snapshots remain frozen computed totals with `reportingCurrency` stamps*) → **3** Space/User reporting currency → **4** currency selector & UX. Provider expansion moved out of MC1 to the provider track; FX P&L (realized/unrealized) is a future capability gated on a cost-basis/lot model, outside MC1 | `docs/initiatives/mc1/MC1_MULTI_CURRENCY_ROADMAP.md`; `docs/initiatives/mc1/MC1_PHASE0_CURRENCY_PROVENANCE_PLAN.md`; `docs/initiatives/mc1/MC1_MULTI_CURRENCY_ARCHITECTURE_CHARTER.md`; `docs/investigations/MULTI_CURRENCY_ARCHITECTURE_INVESTIGATION.md` | — **initiative closed**; **QA Q1–Q6 complete 2026-07-06** (Space-card cross-currency display closed by QA Q5/Q5b; mixed-stamp / FX-miss chart guard by Q4b; currency-switch live-update by Q6), **FX historical backfill applied**, **perf P0** (batched FX reads) landed, **navigation currency-staleness fixed**. Remaining residuals: override expansion beyond the personal dashboard, mixed-currency allocation precision. Next initiative: **OPS-1 — Platform Operations Foundation** |

### Perspective engine track (PE-x — see §4 for the namespace prefix)

`PE-x` is a new track prefix (deterministic perspective/lens layer), allocated per the §4 namespace rule with a folder at `docs/initiatives/pe1/` so the ID cannot be squatted.

| ID | Name | Status | Description | Evidence | Next milestone |
|---|---|---|---|---|---|
| PE1 | Perspective Engine | **Functionally complete** | Deterministic, non-persistent lens layer answering scoped financial questions for one viewing member: typed `LensResult` (one-sentence verdict + headline metric + supporting metrics + explicit assumptions + provenance). Guard-tested invariants (determinism under injected clock; no direct Prisma import; reads exclusively through the KD-19 visibility-enforced data layer). Debt + liquidity lenses shipped; API wired | `lib/perspective-engine/` (engine/index/registry/types, `lenses/{debt,liquidity}.core.ts`, tests `engine`/`debt`/`liquidity`/`route`); `app/api/spaces/[id]/perspectives/route.ts`; `docs/investigations/PERSPECTIVES_INVESTIGATION.md` | Additional lenses + UI surfacing (v2.5) |

### v2.5 foundation initiatives landed (SP-x authorization · EV-x events · Timeline)

Recorded here after they landed on `feature/v2.5-spaces-completion` (between the `37f96f3` and `2db97ef` checkpoints); previously absent from this ledger.

| ID | Name | Status | Description | Evidence | Next milestone |
|---|---|---|---|---|---|
| SP-2 | Spaces authorization centralization | **Complete** | Space authz consolidated behind a single policy predicate; active-member route authorization centralized; public-read exception documented | `lib/spaces/policy.ts`, `lib/spaces/authorize.ts`; tests `lib/spaces/policy.test.ts`, `lib/spaces/authorize.test.ts`; commits `1e06be8`, `11aac36`, `8ede987`, `7442145` | — |
| EV-1 | Domain event seam | **Complete (seam laid)** · expansion deferred to consumers | Typed in-process emit/dispatch with transaction-aware ordering (persist-then-dispatch post-commit). Current shape: one active handler (snapshot regeneration on share change) + several audit-only event types (member removal/leave, Plaid refresh, goal created/checked-in). Not a full event bus yet — consumers arrive with v2.6b | `lib/events/emit.ts`, `lib/events/types.ts`, `lib/events/handlers/snapshot.ts`; commits `9820882`, `4b85c34`, `d01fe27`, `cf487f0` | Add consumers as v2.6b ambient/notification work needs them |
| Timeline Foundation | Member lifecycle + goal check-in activity | **Complete (foundation)** · producers expanding | Real Timeline producers emit member invite/join and goal check-in events into the activity stream, built on EV-1. `lib/timeline-placeholder.ts` still supplies `isPreview: true` demo events for event types without a real producer yet (documents, wallets, AI recommendations, etc.) — badged distinctly, not synthetic history | `lib/timeline-types.ts`, `lib/timeline-placeholder.ts`, `app/api/spaces/[id]/activity/route.ts`; commits `79a8fe5` (T-1), `2db97ef` (T-2) | Replace placeholders with real producers as each event type gains a backend source |
| SP-1 / SP-2.x / SP-2A-4 | Spaces shell unification | **✅ Complete** (2026-07-08) | **One dashboard system for Personal and shared Spaces.** SP-1 template registry + planner; SP-2.1 template-aware create route; SP-2.2 registry-driven Create Space picker; SP-2.3 planner-authority cleanup; SP-2A-3 Personal template materialization; **SP-2A-4a** shell seams (`renderHero` / `initialTab` / host-derived rail) → **4b** `PersonalHero` extraction → **4c** Personal shell flip (`page.tsx` renders `SpaceDashboard`; `PersonalHero` via the `renderHero` seam; legacy `?tab=` deep links mapped) → **4d** `DashboardClient` retirement (**deleted**). Personal is now a **first-class Space rendered through `SpaceDashboard`**. **Doctrine:** **Space → Template → Materialized Sections → Widgets**, with the **Hero as the only sanctioned per-Space divergence** — templates define birth defaults, the planner materializes sections, and user customization can later mutate materialized sections | `app/(shell)/dashboard/page.tsx`, `components/dashboard/SpaceDashboard.tsx`, `components/dashboard/PersonalDashboard.tsx`, `components/dashboard/PersonalHero.tsx`, `lib/space-templates/*`, `lib/space-nav.ts`; test `lib/space-shell-seams.test.ts` (source-scan lock); investigation `docs/initiatives/d9/SP-2A-4_PERSONAL_SHELL_SWAP_INVESTIGATION_2026-07-08.md` | Drag/drop section customization (enabled by the section model, deferred) · SP-3 template provenance (deferred) |

### Schema hygiene track (DB-x — see §4 for the namespace prefix)

`DB-x` is a new track prefix (physical database schema hygiene — legacy physical-name cleanup), allocated per the §4 namespace rule 2026-07-05; folder `docs/initiatives/db1/`.

| ID | Name | Status | Description | Evidence | Next milestone |
|---|---|---|---|---|---|
| DB1 | Physical Schema Cleanup (Workspace → Space database rename) | **✅ Complete** (2026-07-05) · migration `20260705134641_db1_rename_workspace_physical_to_space` applied; Prisma "Already in sync", tsc + lint green | Removed all legacy Workspace-era Prisma `@map` / `@@map` mappings and renamed the underlying physical PostgreSQL objects so the database vocabulary matches the application. **Investigation complete 2026-07-05:** repository inventory found **23 Workspace-era mappings** — 11 `@@map` (6 tables + 5 Postgres enum types) and 12 `@map` columns — **all** legacy Workspace rename; **zero** FinTracker, reserved-SQL-word, Prisma-limitation, or compatibility-only mappings remain. **No runtime application dependency on the physical Workspace names was found** (app / lib / jobs address Prisma model/field names — `Space`, `spaceId` — only; Prisma Client and app code are unchanged by the rename; the sole raw SQL referencing a Workspace physical name targets the already-dropped `WorkspaceAccountShare` table and is orthogonal). Deemed **low-risk** because the project is pre-production (no customers, no external schema consumers) and the change is metadata-only. Full scope + success criteria in §5 (DB1). **Hard constraint:** migration must be hand-authored using `ALTER … RENAME` (metadata-only, data-preserving); Prisma's autogenerated `DROP` / `CREATE` migration is **explicitly prohibited** — it would drop and recreate the renamed tables/columns and destroy data | `prisma/migrations/20260705134641_db1_rename_workspace_physical_to_space`; `prisma/schema.prisma` (0 remaining `@map` / `@@map`); `docs/initiatives/db1/DB1_IMPLEMENTATION_CHECKLIST.md` | — (residual WAS-era dead code/comments cleaned up under LC1) |

### Financial semantics — FlowType (v2.5.5 scope, shipped early in the v2.5 window; folder `docs/initiatives/flowtype/`)

| ID | Name | Status | Description | Evidence | Next milestone |
|---|---|---|---|---|---|
| FlowType | Transaction flow classification (single semantic authority) | **✅ Complete** (P5 closed 2026-07-05) | The v2.5.5 flowType initiative, shipped ahead of its milestone: **write path complete** (P1 classifier → P2 import fidelity → P3 schema + write population, migration `20260703130000_v255_flowtype_schema` → P4 backfill, idempotent + `classifierVersion`-gated) and **read cutover complete** (P5 Slices 0–7: import write completion → Banking/Space flow totals → debt per-liability rollup → AI assembler cutover → annotations gate → chat serializer alignment → cleanup). `classifyFlow` is the single classification entry point; every production writer classifies; Banking, Dashboard Space panel, Debt, AI assembler, Daily Brief, chat serializer, and the annotations gate all read flow. **KD-18 per-liability capability shipped** (Slice 3 rollup + Slice 6 serializer). The legacy `{Income, Interest, Transfer, Payment}` semantic set survives in zero production copies. **Residual debt (named, non-blocking):** (1) merchant-backfill flow-desync seam — `scripts/backfill-merchant-categories.ts` rewrites `category` without invalidating `flowType`. **DATA CERTIFIED CLEAN 2026-07-06** by the Desync Remediation initiative: live audit found **zero** FlowType/Category desyncs (the derived 51-row CC-1 population had already been re-made consistent by the sync write path, which since `275a9c8` classifies `CREDIT_CARD_PAYMENT→Payment` and recomputes flow atomically). Outcome was **certification, not remediation** — no rows changed. The permanent apparatus now guards the property: `npm run audit:flow-desync` + `lib/transactions/flow-desync-invariant.test.ts`. The **architectural** version-gate input-blindspot and the category-rewrite-invalidation contract remain open MI entry-gate items (a passing audit does not close them). See `docs/initiatives/desync/`; (2) `prisma/seed.ts` writes no flow (dev-only; cure: run `backfill-flowtype.ts --apply` after seeding); (3) `FLOW_COST` duplicated in `BankingClient`/`SpaceTransactionsPanel` vs the assembler's `EXPENSE_FLOWS`; (4) `incomeTransactionCount` still counts by category name (undercounts Dividend-INCOME rows) | `docs/initiatives/flowtype/P5_CLOSEOUT_INVESTIGATION_2026-07-05.md` (authoritative closeout); `FLOWTYPE_FOUNDATION_INVESTIGATION.md`; commits `0c89e18`…`f22de52`; flow test suites (`flow-classifier`/`flow-row-input`/`plaid-flow-write`/`plaid-flow-input`) green under D-TEST | — (closed; residual-debt remediation + rewrite-invalidation contract move to the Merchant Intelligence runway) |

### Other

| ID | Name | Status | Description | Evidence | Next milestone |
|---|---|---|---|---|---|
| L-1 | Launch readiness | Planned | Billing, onboarding, Plaid production approval, legal/compliance, ops readiness, support tooling | §5 v3.0 | Start Plaid production application during the v2.6 window (v2.6a–b) |
| UI-1 | Design system (Atlas Glass) | **Active** · in progress (v2.5) | Overlay primitives (`OverlaySurface`/`Dialog`/`FormModal`/`ConfirmDialog`) + Material Engine Phase 1A (per-depth `--glass-filter-*`, Fresnel edge, `floating` tier) landed; modal/chrome migration (M/C/H series) landed; `lib/atlas/palette-ratchet` guard shipped with baseline. The Atlas Liquid card direction shipped and is committed (`AtlasLiquidCard`/`AtlasLiquidCta`, used by `SpacesClient` + Brief surfaces; material vendored at `components/atlas/vendor/liquid-glass/`) — only the browser-wide backdrop-refraction primitive was rejected (§8). Remaining: DataCard Step B consumer migration, primitive material adoption, Phase 1B unified `--atlas-light-angle` light model, Daily Brief visual redesign. Chrome carries brand; data surfaces stay 0%-effect | `components/atlas/*`, `lib/atlas/palette-ratchet.*`, `app/globals.css`, `docs/design-system/ATLAS_GLASS_MATERIAL_DOCTRINE.md`, `docs/design-system/MATERIAL_ENGINE_PHASE_1A_CHECKLIST.md` | Complete adoption; settle single Brief card/CTA architecture |
| PAV | PublishedAccountView | Parked | See §8 | Freeze doc §9.3 | — |
| TI | Transaction Intelligence | **Phase 1 Complete** (2026-07-06) | Phase 1 delivered — includes the **TI-1** canonical transaction row→DTO serialization single-site (replaces the previously duplicated inline mappings), golden-pinned | `lib/transactions/serialize.ts`; `lib/data/transactions.ts`; `lib/transactions/serialize.golden.test.ts` | Phase 2 (scope on the platform runway) |
| MI1 | Merchant Intelligence | **✅ Complete** · M0–M6 (2026-07-07) | Bounded product track in parallel with OPS-4 (MI owns merchant-identity schema + writes; TI owns detail read/presentation; M5 waits for TI P2 as host). **M0** ratification: `docs/initiatives/mi1/MI1_M0_RATIFICATION_2026-07-07.md`. **M1** additive schema only, behavior-neutral: `Merchant`/`MerchantAlias`/`MerchantRule` models (migration `20260707..._mi1_m1_merchant_schema`), `Transaction.merchantId`/`categorySource`/`categoryRuleId` (nullable, SetNull FKs), enums `CategorySource`/`MerchantRuleScope`/`MerchantAliasSource`/`MerchantEnrichmentSource`, `TransactionCategory` +6 (Medical/Entertainment/Transport/PersonalCare/Services/Education — own migration file, the one irreversible statement), and nullable `Merchant` enrichment columns (`website`/`logoUrl`/`enrichmentSource`/`enrichmentConfidence`/`enrichedAt` — storage shape only, no capture wiring). No `MerchantAsset` table, no resolver, no backfill, no read cutover, no UI/API. **M2–M6 shipped (2026-07-07):** M2 resolver (`lib/transactions/merchant-resolver.ts`) · M3 backfill planner + script (`merchant-backfill.ts`, `scripts/backfill-merchant-intelligence.ts`) · M4 shared write-time integration (`merchant-write.ts`; sync + import) · M5 correction loop (`merchant-corrections.ts`, `POST /api/transactions/[id]/correct`) · M6 AI read cutover (assembler groups by `merchantId`/`resolvedMerchant.displayName`). **Post-M6 doctrine + investigations (2026-07-07):** merchant identity semantics ratified-in-draft (a Merchant is the consumer-recognizable counterparty brand at correction granularity; who/what rule; rail taxonomy; safe-merge contract) and the WGU duplicate cluster unified via sanctioned USER merge (`scripts/merge-wgu-merchants.ts`); merge-suggestions/review-queue investigation complete — smallest architecture is a pure on-demand detector + decisions-only persistence + one shared merge core (no auto-merge at any tier) | `docs/investigations/MERCHANT_INTELLIGENCE_READINESS_INVESTIGATION_2026-07-07.md`; `docs/initiatives/mi1/MI1_M0_RATIFICATION_2026-07-07.md`; `docs/initiatives/mi1/MI1_MERCHANT_IDENTITY_SEMANTICS_INVESTIGATION_2026-07-07.md`; `docs/initiatives/mi1/MI1_MERGE_SUGGESTIONS_REVIEW_QUEUE_INVESTIGATION_2026-07-07.md`; `prisma/schema.prisma` (Merchant/MerchantAlias/MerchantRule); `lib/transactions/merchant-schema.test.ts` | MI2 — Merchant Merge Review (planned, see next row) |
| MI2 | Merchant Merge Review | **Planned** | **Purpose:** eliminate future one-off merge scripts by introducing a sanctioned merchant merge workflow. **S1 — Merge Core & CLI:** extract the WGU merge transaction into a shared `lib/transactions/merchant-merge.ts`; generalize `merge-wgu-merchants.ts` into a reusable `merge-merchants` utility; dry-run by default; single authoritative merge contract; no schema; no UI. **S2 — Merchant Merge Review Queue:** deterministic duplicate detector; confidence-tiered suggestions; persist only user decisions (accepted / dismissed); minimal review surface; human-confirmed merges only; no auto-merges; no AI-generated merges; no write-path detection. **Notes:** follows the completed MI identity doctrine; detection is computed on demand, not persisted; suggestions never mutate data; the shared merge core becomes the only sanctioned execution path for merchant merges; PO1 only consumes merge audit events later — ownership remains Merchant Intelligence | `docs/initiatives/mi1/MI1_MERGE_SUGGESTIONS_REVIEW_QUEUE_INVESTIGATION_2026-07-07.md`; `docs/initiatives/mi1/MI1_MERCHANT_IDENTITY_SEMANTICS_INVESTIGATION_2026-07-07.md` (doctrine); `scripts/merge-wgu-merchants.ts` (proven merge contract) | S1 (Merge Core & CLI) — awaits its own ratification |

### Platform operations track (OPS-x — see §4 for the namespace prefix)

| ID | Name | Status | Description | Evidence | Next milestone |
|---|---|---|---|---|---|
| OPS-1 | Platform Operations Foundation | **Active** · S0/S1 landed · **S4/S5/S6 complete 2026-07-07** | The minimum operational floor before any external beta user. **S0/S1 (email substrate):** additive, behavior-neutral, zero production callers — `lib/email/` chokepoint (`sendEmail`), non-throwing `EmailResult`, sender identity map, Resend adapter behind a single grep-enforced SDK import site, credential-free capture transport for dev/test, one generic smoke template. **S4 (rate limiting, 2026-07-07):** production is limited by DEFAULT (`RATE_LIMIT_ENABLED=false` is an explicit, validateEnv-warned emergency opt-out; dev/test stay opt-in); the REAL credentials callback is limited both per-IP (NextAuth route wrapper) and per-identifier (inside `authorize()`, before any lookup — new `limitByKey` helper); KD-3's intentionally-unlimited TOTP setup/disable/recovery-codes closed; per-user limits added to password change, email-change request (alongside its IP limit), and Plaid link-token/exchange-token, with coarse per-user backstops over the per-item cooldown on refresh/sync. Non-enumerating behavior preserved; fail-open on store error unchanged (logged). **S5 (security headers, 2026-07-07):** `headers()` in `next.config.ts` — HSTS (prod only, 180d incl. subdomains), nosniff, `X-Frame-Options: DENY` (enforced), Referrer-Policy, minimal Permissions-Policy, and CSP in **Report-Only** (documented exception; Plaid/TradingView allowances written in; the enforce flip is its own future commit after a clean window). **S6 (ops foundation, 2026-07-07):** `instrumentation.ts` created — `validateEnv()` now actually runs at server boot (prod additionally requires `NEXTAUTH_URL`, `NEXT_PUBLIC_APP_URL`, `RESEND_API_KEY`, `CRON_SECRET`); `GET /api/health` (rate-limited, DB ping + commit sha, no secrets); `startScheduler()` deliberately NOT invoked (OPS-4's decision, noted in the file header); Sentry deliberately not added — `register()` documented as the future init point. Tests: `lib/rate-limit.test.ts`, `lib/env.validate.test.ts`, `lib/security-headers.test.ts`, `lib/security-surface.test.ts` (source-scan guard incl. OPS-4-unstarted tripwire), `app/api/health/route.test.ts` | `docs/initiatives/ops1/OPS1_OPERATIONAL_FLOOR_PLAN.md`; `docs/initiatives/ops1/OPS1_S0_S1_IMPLEMENTATION_PROPOSAL.md`; `lib/email/*`; `lib/env.ts`; `lib/rate-limit.ts`; `next.config.ts`; `instrumentation.ts`; `app/api/health/route.ts` | S9 legal pages / S10 beta access gate; CSP enforce flip after clean report-only window; external uptime monitoring on `/api/health` (ops task, no code) |
| OPS-4 | Background Jobs & Platform Reliability | **✅ COMPLETE (2026-07-07)** · S0–S6 | Make scheduled work observable before making it sophisticated. **S0:** initiative ratified; rulings frozen (JobRun append-only · no retention yet · cron routes unchanged incl. per-route CRON_SECRET · notification cleanup keeps riding process-deletions (F7 stands) · dispatcher deferred to S2 · retry framework later (F16 substrate untouched) · AI scheduling stays v2.6b · telemetry stays PO1) — `docs/initiatives/ops4/OPS4_S0_RULINGS.md`. **S1:** append-only `JobRun` execution ledger (migration `20260707223000_ops4_s1_jobrun_ledger`; strings-not-enums; summary carries counts/kinds/IDs only — never user content or monetary values) + `runJob()` wrapper (`lib/jobs/run.ts`, the single grep-enforced JobRun writer; best-effort non-throwing ledger writes; result/error passthrough — behavior byte-identical); all three cron bodies wrapped (`process-deletions` ledgered as one run incl. its OPS-3 cleanup tail). Tests: `lib/jobs/run.test.ts` (pure injected-client suite + R3/R4/banned-infrastructure source scans). **S2 (dispatcher, 2026-07-07):** typed registry (`lib/jobs/registry.ts` — the three pre-S2 jobs at their exact pre-S2 UTC slots; composed process-deletions body incl. OPS-3 cleanup tail single-sited here, shared by dispatcher + fallback route) · dispatcher (`lib/jobs/dispatch.ts` — half-hour slot matching robust to late cron fire; sequential registry order; per-job isolation, never throws; every run through `runJob()`, S1 JobRun names unchanged) · single CRON_SECRET-guarded cron endpoint `app/api/jobs/dispatch` (any job failure → 500, preserving the Vercel failure signal); `vercel.json` 3 → 1 entry (`0,30 6-7 * * *`); per-job routes retained as manual/fallback + individual-revert targets; **`jobs/scheduler.ts` retired (deleted)** — KD-14's "entrypoint never invoked" limbo closed (stubs remain v2.6b); F7 dissolved (dispatcher exists). Tests: `lib/jobs/dispatch.test.ts` (slot matching, isolation, registry integrity, structure grep-proofs); OPS-3 `cleanup.test.ts` scans updated to the S2 layout (F7 intent unchanged). Closeout: `docs/initiatives/ops4/OPS4_S2_DISPATCHER_CLOSEOUT.md`. **S3 (workload migration, 2026-07-07):** three registrations on the 07:30 slot (already covered by the single cron — zero vercel.json changes): `notification-cleanup` (relocated off the process-deletions tail exactly as the OPS-3 headers promised — **process-deletions single-purpose again**, S2 composed body deleted; isolation now structural via own JobRun) · `purge-trash` (**first production scheduling ever** — the 7-day goal-trash retention promise is now true; body unchanged, now returns its count for the ledger) · `rate-limit-sweep` (new `jobs/sweep-rate-limits.ts` — WHERE-guarded 24h-cutoff deleteMany bounding the never-swept RateLimit table). **Deferred with recorded reasons** (closeout §entry-investigation): digests (vocabulary only — no template, no frequency preference, no already-digested marker; schema+design surface, stays OPS-3-owned) · snapshot cadence (stale-balance semantics unresolved — daily sync refreshes transactions, not balances). Tests: `lib/jobs/s3-workloads.test.ts` (sweep behavior, registry facts, deferral tripwires); dispatch/cleanup suites updated. Closeout: `docs/initiatives/ops4/OPS4_S3_CLOSEOUT.md`. **S4 (notification retry, 2026-07-07):** `jobs/retry-notifications.ts` — the single consumer of the OPS-3 F16 outbox, registered as `notification-retry` (07:30 slot, sequenced after cleanup so aged-out rows are never re-mailed; no new cron). Policy: retry only `status="error"` under `MAX_DELIVERY_ATTEMPTS=3` (1 create-time + 2 retries; fixed daily cadence, no backoff — R6); sent/captured/skipped and at-cap rows never touched (at-cap = the queryable dead-letter state); archived/expired/read/no-recipient rows closed as `skipped` with no increment/send. **Claim-first duplicate-send prevention** (conditional updateMany on observed attempts before the send; residual crash-window documented in the closeout). Same `emailNotificationAdapter` as create.ts (no second email path); ChannelResult recorded verbatim, `deliveredAt` only on `sent`; Notification rows/dedupe untouched. Tests: `lib/jobs/notification-retry.test.ts` (25 pure checks incl. claim-race and idempotent re-runs). Closeout: `docs/initiatives/ops4/OPS4_S4_CLOSEOUT.md`. **S5 (dead-job detection, 2026-07-07):** `lib/jobs/health.ts` — single read-only detector over the JobRun ledger: per registered job, `never-ran` → `overdue` (> `expectedEveryHours`(default 24) + 2h grace) → `failing` (3 consecutive failures; stale `running` >2h counts as a crash, recent running is in-flight) → `healthy`; pure `classifyJobHealth` + `checkScheduledJobHealth` over a narrow read seam; `ScheduledJob.expectedEveryHours` added (health-only — dispatcher provably never reads it); operator CLI `scripts/check-job-health.ts` (nonzero exit when unhealthy). **`/api/health` deliberately unextended** (its frozen OPS-1 header bans public job state; pinned-keys test intact) — recorded in the closeout, along with the deliberate omission of the investigation's email-on-absence leg (detect-only fence; alerting is PO1 Phase 5 / future). Tests: `lib/jobs/job-health.test.ts` (25 pure checks). Closeout: `docs/initiatives/ops4/OPS4_S5_CLOSEOUT.md`. **S6 (operational closeout, 2026-07-07 — documentation only, zero runtime/schema change):** `docs/operations/BACKGROUND_JOBS_RUNBOOK.md` (7-job/4-slot schedule, verification, failure handling, manual recovery) · `docs/operations/KEY_ROTATION_RUNBOOK.md` (CRON_SECRET / RESEND / Plaid / NEXTAUTH_SECRET / ENCRYPTION_KEY with rotation order, downtime, validation; resolves INCIDENT_RESPONSE_RUNBOOK §7.6; the ENCRYPTION_KEY re-encryption *script* is a named gap with its shape fixed in the runbook) · `docs/operations/OPS4_PRODUCTION_READINESS_CHECKLIST.md` · SECURITY_CHECKLIST/INCIDENT_RESPONSE_RUNBOOK OPS-4 references resolved. **Operator residue (non-blocking, named in S6 closeout):** rotation script + drill, preview/prod key-separation confirmation, first-week ledger observation; SEC-1 v1 read-branch removal stays behind its own gate. Closeout: `docs/initiatives/ops4/OPS4_S6_CLOSEOUT.md` | Investigation: `docs/initiatives/ops4/OPS4_BACKGROUND_JOBS_INVESTIGATION_2026-07-07.md`; `docs/initiatives/ops4/OPS4_S0_RULINGS.md`; S2–S6 closeouts in `docs/initiatives/ops4/`; `lib/jobs/*`; `jobs/{sweep-rate-limits,retry-notifications}.ts`; `scripts/check-job-health.ts`; `app/api/jobs/dispatch/route.ts`; `vercel.json`; `docs/operations/{BACKGROUND_JOBS_RUNBOOK,KEY_ROTATION_RUNBOOK,OPS4_PRODUCTION_READINESS_CHECKLIST}.md` | — (initiative closed; alerting/rollups → PO1) |

## 4. Initiative naming — alias table

The D-number collision is resolved by **freezing, not renumbering**. `PHASE_2_DECISION_MATRIX.md` is the sole authority for D1–D14, forever. Historical commits/docs are not edited.

| Historical label (commits, code comments, docs) | Canonical ID | Note |
|---|---|---|
| "D6", "D6.3", "D6.3A–D" in AI/intelligence contexts | **AI-3** | Collision victim: matrix D6 = ProviderCatalog. AI-3 investigations were filed in `docs/investigations/D6_3*.md` because `initiatives/d6/` was already owned by ProviderCatalog |
| "D4 Slice 1", "D4 chat", "D4 provider lifecycle", "D4 Balance Freshness" | **AI-1 / AI-2** (AI work) + D2 (provider lifecycle) | Code used "D4" as an era label broader than matrix D4 (enforcement + agentScope) |
| "D6/D7 Provider Catalog — Slice 1" (`lib/providers/catalog.ts`) | **D6/D7** | Correct usage — matrix meaning |
| "D6 Institution Catalog investigation" | **D6** | Correct usage |

**Namespace rule going forward:** new initiatives get a **track prefix + number**, allocated only in this file, with a `docs/initiatives/<id>/` folder created at allocation time so an ID can never be squatted twice. Track prefixes: `AI-x` (intelligence), `UI-x` (design system), `L-x` (launch/ops), `MC-x` (multi-currency / money-model — allocated 2026-07-03, first member `MC1`), `PE-x` (deterministic perspective/lens layer — allocated 2026-07-03, first member `PE1`), `DB-x` (physical database schema hygiene / legacy physical-name cleanup — allocated 2026-07-05, first member `DB1`), `OPS-x` (platform operations foundation — external-world substrate: email, observability, legal, beta gate — allocated 2026-07-06, first member `OPS-1`), `MI-x` (merchant intelligence — persisted merchant identity, aliases, rules, category provenance — allocated 2026-07-07, first member `MI1`). This is recommended over a single flat namespace (`AI-x` alone or continuing D-numbers) because the flat D-namespace is exactly what collided: unrelated tracks competed for adjacent integers. Prefixes make the track self-evident in a commit message.

## 5. Roadmap

Phases are gated by **exit criteria**, not feature lists. The roadmap ends at launch; everything beyond it lives in §8.

### v2.4 — Architecture Foundation — **MERGED & TAGGED `v2.4.5` (`6b517fa`)**
Shipped: provider lifecycle + Plaid sync/reconcile; AI-1..AI-3; D11/D13/D14; SAL dual-write; admin diagnostics; expand-history workflow. Both critical transaction-visibility leaks — KD-1 (AI context) and KD-15 (UI reads) — are fixed and committed; AI-4 shadow validator and KD-13 hygiene landed; KD-4 SAL write atomicity (Phases 1–3) fixed and committed.
Exit action complete: `feature/phase-2-architecture` was merged and the tag `v2.4.5` cut (merge commit `6b517fa`). No architectural work outstanding — remaining defects are gated to v2.4.5+ and are not v2.4 blockers.

### v2.4.5 — Stabilization / Verification — **TAGGED `v2.4.5` — with carry-forward verification debt** (production-readiness gate)
Scope: fix BALANCE_ONLY transaction leak (KD-1) ✅ → LLM output validator live enforcement (AI-4 / KD-2) ✅ → test suites (merchant normalization, window/rollup math, privacy sanitizers, follow-up heuristics) → `db.$transaction` around merges/route write-groups (KD-4) ✅ → HOME partial-unique index (KD-5) ✅ → observability counters (fallback hits, sync stats, LLM tokens) → rate limiting (auth + chat, KD-3) ✅ → repo hygiene → **category rollup sign asymmetry (KD-17 — absorbed 2026-07-02 per approved roadmap revision)** ✅ (committed `3988510`) → **attribution honesty guardrail, generalized (KD-18 — absorbed 2026-07-02; guardrail only, capability deferred to v2.5.5)** ✅ (committed `3988510`) → replace the raw "Too many messages (max 50)" body-guard error with a user-facing message (copy-level only; graceful compression itself is AI-5/v2.6a).
**Exit criteria:** privacy regression tests green · a reply quoting a number absent from context is detectably flagged · zero non-transactional multi-write flows · D2 fallback counters observable · rate limits live · per-month category totals reconcile to that month's `expenseTotal` by **checked** invariant (KD-17), not prompt prose · per-account attribution questions are answered answer-first — the available deterministic totals/breakdowns first, then an explicit non-attribution disclosure for the missing dimension, never a fabricated per-account table (KD-18, refined `ea223af`).
**Carry-forward debt (tag cut ahead of these — not complete):** of the named test suites, merchant normalization **now exists** (`lib/transactions/merchant.test.ts`, alongside the flow suites `flow-classifier`/`flow-row-input`/`plaid-flow-write`/`plaid-flow-input`); window/rollup math and the route's follow-up/drilldown heuristic suites are still absent. **Caveat (closed 2026-07-05 by D-TEST, `fcadc2a`):** a unified test runner now exists — `npm test` → `scripts/run-tests.ts`, CI-enforced via `.github/workflows/ci.yml` — so registered suites are runner-driven and CI-green, no longer hand-run scripts. Observability counters (fallback hits, sync stats, LLM tokens) are not implemented; the "max 50" user-facing copy fix is unshipped; `.env.example` still omits `AI_OUTPUT_VALIDATION_MODE`/`RATE_LIMIT_ENABLED`/`RATE_LIMIT_SHADOW`. These are tracked as v2.4.5 debt to be closed in the v2.5 window, not as done.
**Hard rule (noted, and partially overtaken by events): the tag was cut and v2.5 opened before the above debt closed.** Fourth Meridian is not production-ready until this debt is retired.

### v2.5 — Spaces Completion + Design Foundation — **IN PROGRESS** (active on `feature/v2.5-spaces-completion`)
Scope: SAL read-cutover everywhere ✅; **WorkspaceAccountShare retirement ✅ (v2.5-A Phase 4c, 2026-07-03, `e17c699` — see D3)**; legacy `Account` out of all read paths (legacy `Account` itself is intentionally retained — its retirement is a separate future milestone pending approval); members/roles polish; visibility tiers enforced in every assembler; production shared-Spaces UX; Space-level AI surfaces; UI-1 tokens + component library, with all **new** surfaces built in the new design language (monolith component decomposition rides along — **not yet started**; the large dashboard components remain unsplit). Stretch: Timeline/Activity — **foundation landed** (see the SP-2/EV-1/Timeline table in §3); Documents still deferred.
**Landed this window (between `37f96f3` and `2db97ef`):** SP-2 authorization centralization (Complete), EV-1 domain event seam (Complete — seam laid), Timeline Foundation (member lifecycle + goal check-ins), Daily Brief redesign + tracked-accounts detail, and the Atlas Liquid card redesign — all committed; see §3.
**Spaces shell unification — ✅ COMPLETE (2026-07-08):** the two-dashboard split is closed. Personal is now a **first-class Space rendered through `SpaceDashboard`** — the Personal branch of `app/(shell)/dashboard/page.tsx` renders the shared shell with `PersonalHero` injected through the `renderHero` seam — and the legacy **`DashboardClient` is retired (deleted)** (SP-2A-4d). **One dashboard system now serves Personal and shared Spaces.** Settled doctrine: **Space → Template → Materialized Sections → Widgets**, with the **Hero as the only sanctioned per-Space divergence.** Templates define birth defaults; the planner materializes sections; user customization can later mutate materialized sections (drag/drop is now *enabled* by the standardized section model — deferred, not next). Sequence: SP-1 → SP-2.1/2.2/2.3 → SP-2A-3 → SP-2A-4a→4d (see the SP-x foundation table in §3). SP-3 template provenance remains deferred.
**Design-foundation reality (in progress):** the UI-1 Atlas Glass work is the live effort — overlay primitives + Material Engine Phase 1A landed (see UI-1 in §3); the **Perspective Engine (PE1)** shipped as the first Space-level deterministic AI surface (see PE1 in §3); the **Daily Brief visual redesign** has landed and is committed. The **Atlas Liquid card direction shipped and is committed** (`AtlasLiquidCard`/`AtlasLiquidCta`, used by `SpacesClient` + Brief surfaces; material vendored at `components/atlas/vendor/liquid-glass/`, no npm deps); only the browser-wide **backdrop**-refraction primitive was evaluated and rejected — Atlas stays on the CSS material simulation for that (parked in §8). Also carries the v2.4.5 verification debt above.
**Flagship UX initiative:** **D2.x — Initial Sync Experience & Historical Pipeline** (see its own block below) — **✅ complete 2026-07-05**. The flagship onboarding-experience initiative for this window — first-connection seamlessness, delivered as a headline v2.5 quality lever alongside the design-system rollout. In-scope deferrals (auto-replay, SyncJob/webhook/retry, AI/Brief sync-health consumption) tracked under v2.5 / v2.6b.
**Exit criteria:** zero reads of `WorkspaceAccountShare` ✅ (model dropped, Phase 4c) · zero legacy-`Account` queries in AI/read paths — **not yet met:** three runtime read sites remain (`lib/imports/authorize.ts`, `app/api/admin/overview/route.ts`, `app/api/accounts/[id]/transactions/route.ts`) · BALANCE_ONLY guarantee proven by tests in a two-user shared Space, end to end ✅ (KD-1/KD-15/KD-19 two-user proof green) · new surfaces ship in the new design system.

### DB1 — Physical Schema Cleanup (Workspace → Space database rename) — **✅ COMPLETE (2026-07-05)**
**Closeout (2026-07-05):** the physical Workspace → Space rename is applied and validated. Migration `20260705134641_db1_rename_workspace_physical_to_space` (hand-authored, metadata-only `ALTER … RENAME` — no destructive DDL) renamed all 6 tables, 12 columns, 5 enum types, and the associated indexes/constraints; the 23 `@map` / `@@map` annotations were removed from `prisma/schema.prisma`. Verified: **zero** physical Workspace tables/columns/enum types/indexes/constraints, Prisma reports "Already in sync", `tsc --noEmit` and `lint` pass, application runs. Residual `WorkspaceAccountShare`-era dead code (unused `dualWriteFrom*` helpers, a vestigial seam gate, stale comments) was cleaned up under the follow-up **LC1 — Workspace Retirement Cleanup**. Planning record: `docs/initiatives/db1/DB1_IMPLEMENTATION_CHECKLIST.md`.
*Initiative, not a version.* Architecture cleanup that finished the Workspace → Space rename at the physical database layer while the project is still pre-production, before Ambient Intelligence (v2.6). The application already spoke "Space" end to end; the physical PostgreSQL objects carried Workspace-era names behind Prisma `@map` / `@@map` until this initiative. Investigation and implementation both complete 2026-07-05 (see Closeout above).
**Investigation results (2026-07-05):** repository inventory found **23 Workspace-era mappings** — 11 `@@map` (6 tables: `Workspace`, `WorkspaceMember`, `WorkspaceInvite`, `WorkspaceGoal`, `WorkspaceDashboardSection`, `WorkspaceSnapshot`; 5 Postgres enum types: `WorkspaceType`, `WorkspaceCategory`, `WorkspaceDashboardTab`, `WorkspaceMemberRole`, `WorkspaceMemberStatus`) and 12 `@map` columns (`workspaceId` on 10 models plus `preferredWorkspaceId` on `User` and `ownerWorkspaceId` on `FinancialAccount`). **All 23 are legacy Workspace rename; zero FinTracker, reserved-SQL-word, Prisma-limitation, or compatibility-only mappings remain.** No runtime application dependency on the physical Workspace names was found — application, lib, and job code address Prisma model/field names (`Space`, `spaceId`) only, so the Prisma Client surface and all app code stay unchanged by the rename; the only raw SQL touching a Workspace physical name targets the already-dropped `WorkspaceAccountShare` table and is orthogonal. Migration deemed **low-risk** because the project is pre-production (no customers, no external schema consumers) and the change is metadata-only.
**Scope:**

- Remove all legacy Workspace-era Prisma `@map` / `@@map` mappings.
- Rename physical PostgreSQL tables `Workspace*` → `Space*`.
- Rename physical columns `workspaceId` → `spaceId` (and the related owner/preferred fields: `ownerWorkspaceId` → `ownerSpaceId`, `preferredWorkspaceId` → `preferredSpaceId`).
- Rename the Workspace-era PostgreSQL enum types to their Space-era names.
- Rename indexes and constraints so no Workspace-era identifiers remain.
- End state: zero Workspace-era physical database names anywhere.
- Prisma Client and application code remain unchanged (model/field names are already Space-era).
- **Migration must be hand-authored using `ALTER … RENAME` (metadata-only) — Prisma's autogenerated `DROP` / `CREATE` migration is explicitly prohibited**, because letting Prisma diff the removed `@map`s produces destructive drop-and-recreate DDL (data loss).

**Success criteria:**

- Zero remaining Workspace physical names (tables, columns, enum types, indexes, constraints).
- Zero `@map` / `@@map` entries related to Workspace in `schema.prisma`.
- No runtime application changes.
- No data loss; existing data preserved (metadata-only `ALTER … RENAME`).
- Validation green: `prisma generate`, hand-authored `prisma migrate`, `tsc --noEmit`, `lint`, and targeted smoke testing.

### D2.x — Initial Sync Experience & Historical Pipeline — **✅ COMPLETE (2026-07-05) · ⭐ v2.5 FLAGSHIP UX INITIATIVE**
**Closeout:** all in-scope slices shipped and both migrations applied (`d2x_slice4_snapshot_isestimated`, `d2x_sync_issue_integrity_gate`) — see `docs/initiatives/d2x/D2X_CLOSEOUT_AUDIT.md`. Delivered: **Slice 1** fast-path split · **Slice 2** background history via `after()` · **Slice 3** `GET /api/sync/status` + permanent `/dashboard/connections` hub (canonical Liquid `ConnectionCard`, Connect-institution + Add-wallet actions) · **Slice 4** 30-day `SpaceSnapshot` backfill (`isEstimated` + chart badge) · **Slice 4B** credit-card debt reconstruction · **snapshot orchestration fix** (all-items refresh snapshots each Space once after all institutions, excludes failed-item Spaces) · **transaction-sync integrity hardening** (`removed[]` soft-delete + resurrection; `recover-plaid-item-transactions.ts`) · **Data Integrity Gate M1** (`SyncIssue` table) **+ M2** (balance↔transaction reconciliation, flag-only). The progressive-reveal contract is in place for whatever surfaces exist at the time. **Deferred to their tracks (non-blockers):** M2 auto-replay + SyncJob / Plaid `SYNC_UPDATES_AVAILABLE` webhook / retry hardening → **v2.5**; snapshot `quality`/provenance column, AI/Brief sync-health + estimated-marker consumption ("Chase history may be incomplete"), `pending_transaction_id` reconciliation, fingerprint over-match hardening → **v2.6b (Ambient Intelligence)**; deeper-than-30-day history → **Snapshot Backfill initiative**. Daily Brief generation → v2.6b; richer AI → AI-x (own tracks). *Historical note below retained as the original approval record.*

*Initiative, not a version.* Approved 2026-07-03. **Flagship UX initiative for the v2.5 window** — a new user's first five minutes shapes perceived quality more than any single visual pass, and the underlying architecture (730-day depth, idempotent sync, snapshot backfill) already exists; this initiative is about *exposing* it as one seamless moment rather than three disconnected events with a Refresh button between them. Lands in the v2.5 window, **queued after the current D2 import/sync work and before AI indexing (AI-x advisor tracks, v2.6a+) and later analytics work (v2.5.5)**. Orchestration + UX only — reuses the existing sync engine (`syncTransactionsForItem`), never replaces it. `days_requested=730` already shipped; the target is availability latency, not depth. Approved phase order: **0** investigation (complete) → **1** split the fast path (balances/accounts/ready-slice/today's snapshot return immediately) → **2** background historical continuation via `after()`/`waitUntil`, no queue/`SyncJob` yet → **3** lightweight sync-status endpoint + UI polling (states inferred from existing `PlaidItem` fields) → **4** trigger the 30-day `SpaceSnapshot` backfill after history completes (depends on the Snapshot Backfill initiative; non-blocking) → **5** optional `SyncJob` model later → **6** Plaid `SYNC_UPDATES_AVAILABLE` webhook (cron stays the fallback) → **7** retry hardening. No schema unless genuinely needed; every phase independently shippable and revertible; best-effort/non-fatal. Charter: `docs/initiatives/d2x/D2X_INITIAL_SYNC_EXPERIENCE_CHARTER.md`.
**Scope boundary (what "done" means):** D2.x delivers the **orchestration / progressive-reveal contract** — the pipeline and status surface that let each downstream capability light up automatically the moment its data dependency is ready. The *individual surfaces themselves* ship on their own tracks (Daily Brief generation → v2.6b Ambient Intelligence; richer AI analysis → AI-x). D2.x's exit is therefore that the reveal is seamless and automatic **for whatever surfaces exist at the time**, not that every insight is present on day one — otherwise its exit would be gated on v2.6b work and could not close in the v2.5 window.
**Exit criteria:** A first-time connection displays current balances within seconds. Historical transactions continue automatically in the background without any user action. Progress is visible ("Importing history…"), the user never needs to press Refresh, and the Daily Brief, 30-day chart, AI analysis, and historical insights automatically become available as each dependency completes — all without a duplicate sync pipeline and with the daily cron intact as fallback.

### MC1 — Multi-Currency Architecture — **✅ INITIATIVE COMPLETE (2026-07-05) · 🏛️ FOUNDATIONAL ARCHITECTURE INITIATIVE**
**MC1 QA + follow-ons complete (2026-07-06).** Post-closeout QA cleanup **Q1–Q6** shipped (mislabeled conversions, un-threaded aggregates, itemized-row currencies, the SpaceDashboard family, per-Space **Space-card** currency including the **Q5b** off-stamp read-time conversion fix, and **Q6** currency-switch live-update). The **FX historical archive backfill** is applied (`scripts/backfill-fx-rates.ts` from the earliest `SpaceSnapshot` date), so pre-cron history converts instead of degrading. **Performance P0** batched the FX archive prefetch in `buildConversionContext` (one range read per window replaces N sequential per-date reads; conversions byte-identical, pinned by `lib/money/context-batch.test.ts`). The **navigation currency-staleness** bug (the previous Space's currency following into the next until a manual refresh) was investigated and **fixed** — page-scoped `DisplayCurrencyProvider` from the freshly resolved Space context, `router.refresh()` parity on the Spaces-grid switch, and `spaceId` host keying. No stored-fact mutation; all-USD behavior unchanged. Investigations under `docs/initiatives/mc1/`: `MC1_QA_Q4B_INVESTIGATION_2026-07-06.md`, `MC1_QA_CURRENCY_PROPAGATION_AUDIT_2026-07-05.md`, `MC1_QA_PERFORMANCE_INVESTIGATION_2026-07-06.md`, `MC1_NAV_CURRENCY_STALENESS_INVESTIGATION_2026-07-06.md`.
**Phase 4 (selector & UX) delivered 2026-07-05 — closing MC1.** Slices 1–9: display-currency provider/hook over the constant seam (aggregate labels only; itemized stays native); Space selector in ManageSpaceModal (allowlist, forward-only confirmation copy, audit from/to) + User default in Settings→Profile ("new Spaces only"); quiet "≈ est." presentation reusing the PerspectivesWidget marker across hero/banking/debt/lens/investments/holdings aggregates; stamp-aware chart readers (homogeneous fast path; off-stamp points converted at each snapshot's own date; one badge, both causes; rows never rewritten); holdings assembler + surfaces converted (F-5); Space-panel API `moneyCtx` payload (F-6); AI serializer currency label + flag-gated single estimation disclosure with pinned-wording tests; ephemeral "View as" override (in-memory only, read-only endpoint, writers never consult it). **Full-initiative guarantees re-proven at closeout:** no converted/normalized columns anywhere; Transaction/Holding amounts native; snapshots stamped, never rewritten; ~10 MC1 test suites green. Residual ledger (final closeout §5): Space-card cross-currency display; override expansion beyond the personal dashboard; mixed-currency allocation precision (donut/concentration); F-7 DebtClient utilization sums; chart polish; Transaction Intelligence + Merchant Intelligence integration as future initiatives; FX P&L gated on a future lot model. Closeout: `docs/initiatives/mc1/MC1_FINAL_CLOSEOUT_REPORT_2026-07-05.md`.
**Phase 3 (reporting currency — THE FLIP) delivered 2026-07-05** — Slices 1–7: `Space.reportingCurrency` (authoritative) + `User.reportingCurrency` (copy-once, no retroactive inheritance; migration `20260705221000`, `257f63f`) with PATCH allowlist; server context factory + client-safe serialize/rehydrate; `estimated` flags threaded additively through every aggregate (data-only); **snapshot flip** (stamp = context target, atomic; backfill converts each reconstructed day at its own rate; history never rewritten); **AI flip** (assemblers + chat rollup on real contexts; "summed without conversion" notes retired); **liquidity lens flip** (injected-clock valuation; db-import tripwire preserved via `buildSpaceConversionContextById`); **client surfaces flip** (serialized context props → DashboardClient/KpiRow/Banking/Transactions panel/DebtClient; bundle purity grep-proven). All-USD Spaces numerically identical at every slice (equivalence gates, ~110 MC1 checks); rollback = reset Spaces to USD (data-level, no deploy) or per-seam identity revert. **Phase 4 entry findings (closeout §4):** snapshot currency-estimation flag decision; mixed-stamp chart display; F-5 holdings assembler still raw; F-6 SpaceDashboard panel context-less (API-payload decision); estimated-flag presentation; selector UX. Closeout: `docs/initiatives/mc1/MC1_PHASE3_CLOSEOUT_REPORT_2026-07-05.md`.
**Phase 2 (read-time conversion) delivered 2026-07-05** — Slices 1–5: pure money core `lib/money/` (`convertMoney`/`convertAndSum`/`identityContext`; miss/null-residue ⇒ native + `estimated`, never exclude, never throw; no rounding — `030dc07`), async context builder over the FxRate archive (frozen sync lookup table, fx-service parity, `581bf65`), balance family threaded (`classifyAccounts` + snapshot writers + AI accounts assembler; client components context-less), transaction family threaded (`lib/debt.ts` rollups, AI accumulators + `buildMonthlyBreakdown`, chat per-liability rollup; read-only `currency` select extension). **Target remains USD** via `identityContext(DEFAULT_DISPLAY_CURRENCY)` at exactly 6 sites — `buildConversionContext` has zero product callers; 22 byte-identical golden checks (incl. mixed-currency fixtures) pin neutrality; `lib/money` structurally write-free. **Phase 3 entry findings recorded** (closeout §4): F-1 client-computed totals need server totals/serialized rates; F-2 snapshot context target + `reportingCurrency` stamp must switch together; F-3 liquidity lens raw sums need threading; F-4 merchant/recurring rollups intentionally native pending cadence design. Closeout: `docs/initiatives/mc1/MC1_PHASE2_CLOSEOUT_REPORT_2026-07-05.md`.
**Phase 1 (FX provider layer) delivered 2026-07-05** — Slices 1–4: `FxRate` immutable dated archive (migration `20260705201500`, `8689e2d`), pure core library `lib/fx/` (resolution service: identity fast path, USD cross-rate, ≤7-day walk-back, `RateMiss` as value; `fa7f196`), providers + failover (`openexchangerates` primary w/ `OXR_APP_ID`, `frankfurter` ECB-subset failover with the no-forged-close weekend rule) + offline-dry-run backfill `scripts/backfill-fx-rates.ts`, and the daily Vercel Cron `GET /api/jobs/fetch-fx-rates` (06:30 UTC, Hobby slot #2, CRON_SECRET). **Live-validated against OXR**: 4-day backfill, 96 rows, source-attributed, `--verify` 0 mismatches, append-only re-run confirmed. Zero consumers — behavior-neutral by contract; conversion is Phase 2. 71 fx unit checks green. Residual debt (named, non-blocking): 365-day initial depth cap; SAR/AED absent on failover-covered days until next OXR-healthy top-up; FX failures console-logged (no SyncIssue kind); `tsx` scripts need `OXR_APP_ID` exported. Closeout: `docs/initiatives/mc1/MC1_PHASE1_CLOSEOUT_REPORT_2026-07-05.md`.
**Phase 0 (currency provenance) delivered 2026-07-05** — Slices 1–4: schema + migration `20260705191500_mc1_phase0_currency_provenance` (`298ef56`), writer stamping across all 7 production write paths (`1aa342b`), idempotent derivation backfill `scripts/backfill-currency.ts` (`bf53507`), closeout `docs/initiatives/mc1/MC1_PHASE0_CLOSEOUT_REPORT_2026-07-05.md`. Behavior-neutral: zero readers of the new fields; row-level nulls mean "denomination never recorded", never "assumed USD". Residual debt (named, non-blocking): seed/test-fixture rows unstamped (cure: run the backfill after seeding); `FinancialAccount.currency` not re-asserted on refresh (deferred to Phase 1/2 entry). Dev-DB exit checks pending per closeout §5.
*Initiative, not a version. **Not a v2.5 deliverable.*** Approved 2026-07-03. A long-term architectural initiative that evolves Fourth Meridian from a USD-first platform into a first-class multi-currency financial system **without future rewrites**. **Queued after the D2.x Initial Sync Experience work and the Snapshot Backfill initiative, and before future provider expansion** — this is a **foundational architecture initiative that future providers (Coinbase, Kraken, Interactive Brokers, Schwab, Fidelity, CSV imports, wallets) depend on**: provider work should build *on* the currency architecture rather than precede it. Approved direction (authoritative design input: `docs/investigations/MULTI_CURRENCY_ARCHITECTURE_INVESTIGATION.md`): **Phase 0 provenance stamping approved** during the USD-only era (additive, behavior-neutral, all defaults `USD`) · **Option B** storage model (store original values + normalized values, frozen at write time) · **reporting currency belongs to the Space** (different Spaces may carry different reporting currencies; chart/AI-session currency is an ephemeral display override only) · **currency exists at the row level** (transactions, holdings, snapshots), **not only accounts** · **historical reporting uses historical FX, never today's rate** · **crypto is modeled as an asset with fiat valuation, not a simple cash currency**. The aggregation chokepoint is `sumBalances()`/`classifyAccounts()` (`lib/account-classifier.ts`) — the conversion cutover isolates there. Phased outline (**restructured & approved 2026-07-05**, superseding the original 0–7 outline — governing doc: `docs/initiatives/mc1/MC1_MULTI_CURRENCY_ROADMAP.md`): **0** currency provenance (provenance only; implementation-ready plan: `docs/initiatives/mc1/MC1_PHASE0_CURRENCY_PROVENANCE_PLAN.md`) → **1** FX provider layer → **2** read-time conversion via shared money service (*read-time preferred over Option B write-time normalized columns for now; conversion never mutates stored financial facts; snapshots remain frozen computed totals with `reportingCurrency` stamps; historical FX and FX P&L are future capabilities, not Phase 0 work*) → **3** Space/User reporting currency → **4** currency selector & UX. Charter (amended 2026-07-05): `docs/initiatives/mc1/MC1_MULTI_CURRENCY_ARCHITECTURE_CHARTER.md`.
**Interactive Brokers note:** IBKR validates the requirement for **row-level currency support** — a single brokerage account can contain assets denominated in multiple currencies, which an account-level currency model cannot represent.
**Roadmap note:** *This initiative intentionally separates **currency provenance** from **currency conversion**. Provenance must be captured first because it cannot be reconstructed later.*
**Exit criteria:** every monetary row (transaction, holding, snapshot) is self-describing about its currency · totals are computed in a Space-selected reporting currency via converted values · historical charts use historical FX and do not shift when today's rate moves · AI context states converted totals while citing originals · the model demonstrably supports a multi-currency provider (IBKR-style mixed-currency account) without schema rewrite.

### v2.5.5 — Financial Intelligence *(added 2026-07-02, approved roadmap revision — `docs/ROADMAP_REVISION_PROPOSAL_2026-07.md`)*
Point milestone: pure data-semantics, **no new product surface** (growth beyond that is scope creep — cut it back).
**Progress note (2026-07-05):** the flowType classification scope — including the KD-18 flow-destination-attribution requirement — **shipped early, in the v2.5 window** (FlowType P1–P5 complete; see the FlowType ledger entry in §3). The remaining v2.5.5 scope (transaction-semantics doctrine tests, metadata depth, cleanup tooling) is unchanged and still owns this milestone.
Scope: flowType classification (`docs/investigations/TRANSACTION_FLOW_CLASSIFICATION_INVESTIGATION.md`) — **now including flow destination attribution** (per-liability debt-payment rollup from destination-side legs; KD-18 capability, ratified 2026-07-02 per `docs/investigations/DEBT_PAYMENT_ATTRIBUTION_INVESTIGATION.md` §4-5; the flowType model must carry destination, not just kind, or the KD-18 gap is rebuilt); transaction-semantics doctrine (explicit sign rules for category rollups, `Fee`/`Groceries` category reachability — KD-17 side findings, category population contracts); transaction metadata depth (`docs/investigations/TRANSACTION_METADATA_DEPTH_INVESTIGATION.md`); transaction cleanup tooling. (KD-6 v1→v2 re-encryption no longer rides here — resolved as SEC-1, code-complete with zero v1 rows; see §7.)
**Exit criteria:** transaction-semantics contracts test-enforced · one canonical aggregation doctrine across summary / monthly / drilldown paths · zero new surface shipped.

### v2.6a — Advisor Intelligence (AI-5) *(v2.6 split 2026-07-02, approved roadmap revision)*
Scope: AI-5 charter (`docs/initiatives/ai5/AI-5_ADVISOR_INTELLIGENCE_PROPOSAL.md`) — conversation-state substrate; active-window + context-change disclosure; confidence/completeness propagation through derived assessments; intent-path consistency (KD-16); graceful context compression (retires the max-50 limit structurally); advisor-quality presentation. KD-8 (master-mode disclosure) rides here.
**Entry criteria:** v2.5 seams closed; KD-17 fixed in v2.4.5 (confidence propagation must not build on corrupted category figures).
**Exit criteria:** the eight observed conversation-quality failures (2026-07 testing session) reproduced as tests and green · no reply changes time window silently · no contradictory data-availability claims across intent paths · every derived metric carries or suppresses its input caveats, verified by test.

### v2.6b — Ambient Intelligence
Scope: scheduler substrate (D5 closure); AiAdvice write path; Daily Brief generation; signals → notifications; AI Inbox; context-priority planner live (prompt budgeting); advisory modes; KD-9, KD-12, KD-14 close here. Start Plaid production application during the v2.6 window (longest external lead time).
**Entry criteria:** v2.6a exit — the system may not speak unprompted until it cannot misquote a number (KD-2 ✅) **and** can hold a coherent conversation when prompted (AI-5).
**Exit criteria:** one week of scheduled briefs with zero validator failures · notification opt-in/out · audit-log growth bounded (fix 2-rows-per-chat-per-Space amplification).

### v3.0 — Launch (L-1)
Scope: billing/subscription (D10 ban lifts here only); onboarding funnel; Plaid production credentials live; legal/compliance posture (ToS, privacy policy, remove "financial advisor" framing, LLM data-processing disclosure — external counsel); tested backups + incident response + alerting; support tooling; UI-1 consistency/perf/accessibility polish pass. **Zero new product surface.**
**Exit criteria:** a stranger can pay, connect a bank, share a Space with a partner, and be supported and recovered.

## 6. Production readiness

**Strengths (real, verified):** deterministic-first AI architecture (pre-computed assessments, complete-months math, confidence gating, provenance doctrine); Plaid identity-instability handling with a convergent, never-hard-deleting merge engine; boundary discipline (single auth chokepoint, single LLM import site, single decrypt module, HKDF purposes, tenancy with no admin bypass); disciplined additive-first migration execution.

**Blockers (must fix before any external user):**

1. BALANCE_ONLY transaction leak: **both paths fixed and committed** — AI context (KD-1) and UI reads (KD-15), 2026-07-02, via the shared canonical predicate `lib/ai/visibility.ts`
2. LLM output validation: **live enforcement fixed and committed** (KD-2, AI-4, 2026-07-02, `27fb872`) — the validator now drives `applyEnforcement` at the chat choke point; default `annotate` caveats any reply with an unreconciled figure, `AI_OUTPUT_VALIDATION_MODE` switches shadow/annotate/block, audit logging preserved, fails open. Residual: membership-based (not provenance-based); `block` gated behind false-positive evidence; `shadow` retained as kill switch (flag not yet in `.env.example`)
3. Rate limiting on auth + chat: **fixed and committed** (KD-3, 2026-07-02, `c0f290b`) — endpoint-local fixed-window limiter (`lib/rate-limit.ts`), flag-gated (`RATE_LIMIT_ENABLED`, `RATE_LIMIT_SHADOW`), **off by default**. Must be enabled in production; several TOTP endpoints remain intentionally unlimited (see KD-3 caveats in §7)
4. Non-atomic `SpaceAccountLink` writes: **fixed and committed** (KD-4, 2026-07-02, Phases 1–3 — helper `tx` threading, merge atomicity, route-level `db.$transaction`). Unenforced HOME uniqueness under concurrency (KD-5) is **now fixed & committed** (`0d6e5d2`) — a DB-level partial unique index plus `P2002` recompute-and-retry closes the `computeLinkKind` count-then-write race; see KD-5 in §7.
5. AI financial-context honesty: **both fixed and committed** (2026-07-02, `3988510`) — KD-17 (category rollup sign asymmetry) makes category totals debit-only with a **checked** ≤-`expenseTotal` invariant at both serialization sites, and KD-18 (fabricated per-account attribution) adds a generalized attribution disclosure + non-attribution doctrine so the model never invents a per-account/per-card breakdown the membership validator structurally cannot catch. Tests: `transactions.kd17.test.ts`, `attribution-guardrail.kd18.test.ts`
6. Thin test coverage (output validator, privacy suites, intent classifier, and now KD-11's gap-gating characterization suite; merchant normalization now exists too — `lib/transactions/merchant.test.ts`) — window/rollup math and the route's follow-up/drilldown heuristic suites still absent. The "no test runner" caveat is **closed** (D-TEST, `fcadc2a`: `npm test` → `scripts/run-tests.ts`, CI at `.github/workflows/ci.yml`). **Note:** `v2.4.5` was tagged (`6b517fa`) ahead of the remaining suites and the observability counters; they remain open as v2.4.5 carry-forward debt (see §5 v2.4.5), so the tag does not imply this blocker is closed
7. LLM data-processing posture undefined (retention terms, user disclosure)

**Known risks:** five migration seams open concurrently; verification debt (invariants asserted in comments, not checked in code); solo-maintainer bus factor; `Float` for money; documentation weight exceeding maintenance capacity (this file is the countermeasure).

## 7. Known defects register

| ID | Issue | Severity | Owner milestone | Status |
|---|---|---|---|---|
| KD-1 | Transactions-summary assembler ignores `visibilityLevel`; BALANCE_ONLY accounts' merchants/amounts enter AI prompts (`lib/ai/assemblers/transactions.ts` SAL filter) | **Critical** | v2.4.5 | **Fixed & committed** 2026-07-02 (`cea8220`) — canonical predicate `lib/ai/visibility.ts` (FULL-only; ad-hoc SHARED audit clean in dev+prod) applied to summary + drilldown; tests: `lib/ai/assemblers/transactions.privacy.test.ts`, `scripts/test-visibility-two-user-space.ts` |
| KD-2 | No deterministic validation of LLM output figures against context | High | v2.4.5 (AI-4) | **Fixed & committed** 2026-07-02 (`27fb872`) — the shadow validator (`lib/ai/output-validator.ts`) is promoted to **live enforcement**. A new pure, deterministic `applyEnforcement(reply, result, mode)` (same module) turns the membership-with-tolerance `ValidationResult` into the reply actually returned; the chat route reads `AI_OUTPUT_VALIDATION_MODE` (`shadow` \| `annotate` \| `block`, **default `annotate`**) and applies it at the single post-`generateChatReply` choke point. `annotate` appends a fixed `UNVERIFIED_FIGURE_NOTICE` once (append-only, idempotent — model text is never edited/deleted) when a figure cannot be reconciled; `block` substitutes a fixed notice carrying no numeric token; `shadow` restores the prior observational behavior. Existing audit logging is preserved (write-only-on-flag `AI_OUTPUT_VALIDATION_FLAGGED`, now also recording the `enforcement` mode — no KD-12 amplification). Validator failures fail open: a swallowed exception yields a CLEAN result so enforcement deterministically no-ops and the reply is unchanged. Prompt/context generation, the provider boundary, and `flowType` are untouched. Tests: `lib/ai/output-validator.test.ts` extended with 9 deterministic enforcement cases (shadow/annotate/block, idempotence, clean pass-through, blocked-notice has no flag-eligible figure, determinism) — suite green (27 cases). Validation: `tsc --noEmit` clean, `lint` clean. Design record: `docs/initiatives/ai4/KD-2_LIVE_ENFORCEMENT_INVESTIGATION.md`. **Accepted caveats:** (1) default mode is **`annotate`** — enforcement is live and user-visible by default; (2) **`shadow` remains available as a kill switch** via `AI_OUTPUT_VALIDATION_MODE=shadow` with no revert, and the flag is not yet documented in `.env.example`; (3) the validator is **membership-based, not provenance-based** — it confirms a figure exists somewhere in context, not that the model chose the *right* one of several valid figures (KD-10 canonicalization removed the known dual-figure case, but this remains the class boundary); `block` is wired and tested but should stay gated behind low false-positive evidence before becoming the default. |
| KD-3 | No rate limiting (auth endpoints, `/api/ai/chat`) | High | v2.4.5 | **Fixed & committed** 2026-07-02 (`c0f290b`) — endpoint-local fixed-window limiter `lib/rate-limit.ts` (`RateLimit` table in prod, in-memory in dev; race-safe upsert on `@@unique([key, windowStart])`; fails open). IP-limited auth endpoints: `pre-login` (10/60s), `register` (5/900s), `forgot-password` (5/900s), `reset-password` (10/900s). User-limited authenticated endpoints (SYSTEM_ADMIN exempt at call site): `ai-chat` (30/60s), `totp-verify` (5/900s). `/api/ai/chat` also carries body guards (MAX_MESSAGES=50, MAX_TOTAL_CONTENT_CHARS=24,000) enforced before context build/LLM call. Validation: `tsc --noEmit` clean, `lint` clean. **Accepted caveats:** (1) limiter is **off by default** — `RATE_LIMIT_ENABLED` must be `true` in production; `RATE_LIMIT_SHADOW` gives log-only measurement; neither flag is yet documented in `.env.example`. (2) TOTP `setup`/`disable`/`recovery-codes` remain intentionally unlimited (TODO comments) — only `totp-verify` is limited. (3) Fixed-window (not sliding) permits up to ~2× burst at window boundaries; IP keying trusts first `x-forwarded-for` hop with an `"unknown"` shared-bucket fallback — acceptable for abuse containment on Vercel. |
| KD-4 | Multi-table `SpaceAccountLink`-related writes (create/restore/archive/revoke/merge/permanent) not atomic; a partial failure could leave orphaned/half-applied state | High | v2.4.5 | **Fixed & committed** 2026-07-02 (`a732986`, `55e0abb`, `23a6387`) — Phase 1 transaction-aware SAL helpers (optional `DbClient`/`tx` threading) complete; Phase 2 merge pipeline atomicity complete; Phase 3 route-level `db.$transaction` wrapping across 9 files complete. No nested interactive transactions; external calls (Plaid `itemRemove`), snapshot regen, and provider-identity mirror all kept outside transactions. Validation passed locally: `prisma generate`, `tsc --noEmit`, `lint`. **Caveat:** `exchangeToken` keeps `FinancialAccount` resolution outside the connection+SAL transaction (forced by the self-transactional fingerprint resolver) — residual orphan window is low severity and retry/self-healing. Note the earlier "WAS↔SAL mirror desync" framing was obsolete: WAS runtime writes were already retired, so KD-4 was multi-table SAL atomicity. KD-5 (HOME uniqueness under concurrency) is **not** solved by KD-4 and remains open. |
| KD-5 | "One HOME per account" unenforced; `computeLinkKind` count-then-write race | High | v2.4.5 | **Fixed & committed** 2026-07-02 (`0d6e5d2`) — DB-level partial unique index `SpaceAccountLink_one_home_per_account` on `(financialAccountId) WHERE kind='HOME'` (additive raw-SQL migration `20260702170000_kd5_home_partial_unique_index`; status-agnostic, matching `computeLinkKind`'s count). `dualWriteSpaceAccountLink` now catches the resulting `P2002`: on the autocommit path it recomputes `computeLinkKind` against the committed winner and retries the upsert once (the loser resolves to SHARED via the same helper, never a hard-coded kind); inside a caller-supplied `tx` it rethrows so the surrounding `db.$transaction` rolls back cleanly. Preflight tooling: `scripts/dedupe-home-links.ts` (dry-run, idempotent, keeps earliest-`createdAt` HOME, demotes extras to SHARED, ties skipped) plus `scripts/verify-space-account-link-backfill.ts` CHECK 2, both of which must run GREEN before the index applies. Validation: `tsc --noEmit` clean, `lint` clean. **Accepted caveats:** (1) graceful recompute-and-retry only on the autocommit path — inside a caller transaction the write aborts and the caller must retry (the DB still guarantees no duplicate HOME); (2) single retry — a rare 3-way race can still surface `P2002` (correctness preserved; the request fails rather than silently duplicating); (3) the migration FAILS if pre-existing duplicate HOMEs are present — `dedupe-home-links.ts` must be run first (fails loud, not silent); (4) identical-`createdAt` ties are skipped for manual review. |
| KD-6 | v1 (root-key) ciphertexts not re-encrypted; D14 Slice 5 pending | Medium | v2.5 | **Resolved (code-complete) as SEC-1** — v1 ciphertext count is **0**, so no re-encryption is required (`docs/investigations/SEC-1_KD-6_INVALID_TOKENS_INVESTIGATION.md`); Slice 5a version classifier `detectCiphertextVersion` shipped and Slice 5c removed the deprecated root-key `encrypt()`/`decrypt()` (`lib/plaid/encryption.ts`, test `lib/plaid/encryption.test.ts`). Only residual: the v1 read-branch removal gate (subtractive, gated on 0 v1 rows across all environments + backup-retention window) |
| KD-7 | 5,000-row desc-ordered fetch cap silently truncates oldest months of long windows while prompt asserts rollups are complete | High | v2.4.5 | **Fixed & committed** 2026-07-02 (`c0f290b`) — deterministic `LIMIT+1` sentinel in `lib/ai/assemblers/transactions.ts`: both the summary and drilldown queries now fetch `TRANSACTION_FETCH_LIMIT + 1`, detect overflow, and aggregate only the capped set. Summary contract (`lib/ai/types.ts`) gains `truncated`, `coverageStartDate` (oldest retained row date), and `fetchLimit`; `MonthlyBreakdownEntry` gains optional `truncated?`, stamped on the coverage-floor month. Consumers made truncation-aware: `computeSpendingTrends` (`annotations.ts`) and the chat average/category builder (`app/api/ai/chat/route.ts`) exclude truncated months exactly like calendar-partial ones; the prompt emits a `COVERAGE LIMIT` caveat and an `[INCOMPLETE month]` flag so the model never presents truncated history as exact or full-window. Drilldown `truncated` now also fires on the fetch cap (not just the display cap). Validation: `tsc --noEmit` clean, `lint` clean. **Accepted caveats:** the 5,000-row cap itself is unchanged by design — this ticket makes truncation *detectable and honest*, not eliminated; full SQL-side aggregation (monthly `date_trunc`, JS merchant-normalizer parity) is deferred as separate follow-up. When drilldown is fetch-truncated, its `matchedTotal`/`totalCount` are a lower bound (flagged via `truncated`), not an exact total. |
| KD-8 | Master-mode chat: unbounded prompt size; failed Spaces silently omitted (`Promise.allSettled`) with no disclosure | Medium | v2.6a (AI-5) | Open |
| KD-9 | Plaid `removed` hard-deletes transactions, contradicting D8 soft-delete rule | Low | v2.6b | Open |
| KD-10 | Two competing "monthly expense" figures in one prompt (assessment block vs context block) | Medium | v2.4.5 | **Fixed & committed** 2026-07-02 (`0d6e5d2`) — single authoritative calculation `computeAverageMonthlySpending(txn)` in `lib/ai/intelligence/annotations.ts` (average of each reliable month's `expenseTotal`; `null` when none). Reliable months come from the shared predicate `reliableMonths(txn)` = complete AND non-`truncated`, reusing KD-7's fetch-cap flag. Both assessment consumers (cash-flow `estimatedMonthlyExpenses`; liquidity `estimatedMonthlyExpense`, which coverage divides into) and the prompt context block (`app/api/ai/chat/route.ts` AVERAGE MONTHLY SPENDING) now call the same two helpers, so context and assessment emit an identical figure. The old window-normalized `expenseTotal/windowDays*30` estimates are removed. Validator test updated (`lib/ai/output-validator.test.ts`): asserts the single authoritative value reconciles and the retired competing figure is flagged. Validation: `tsc --noEmit` clean, `lint` clean, validator suite green. **Accepted caveat:** when no reliable (complete, non-truncated) month exists the value is `null` — the monthly-expense estimate and liquidity coverage are then unavailable rather than approximated (intentional: no fabricated figure for short-history / heavily-truncated Spaces). |
| KD-11 | Duplicated, drifting keyword heuristics between chat route and intent classifier; only classifier is tested | Medium | v2.4.5 | **Fixed & committed** 2026-07-02 (`0d6e5d2`) — authoritative vocabulary module `lib/ai/intent/keywords.ts` is now the single home for the payoff / update-action / update-field keyword sets (shared tokens defined once as `*_SHARED`/`*_CORE`; each consumer's list composed from that core). The chat route's inline arrays and gap-gating functions were relocated to `lib/ai/intent/gap-intent.ts` (exposed via the intent barrel); the classifier's local arrays are replaced by imports of the `*_ROUTING_*` exports. Token sets were verified identical to the pre-KD-11 arrays (all 6 lists), so classifier semantics and route behavior are unchanged and prompt generation is untouched. New characterization suite `lib/ai/intent/gap-intent.characterization.test.ts` (19 cases) pins the route gap-gating behavior including the router↔route drift; `classifier.test.ts` remains green (11 cases). Validation: `tsc --noEmit` clean, `lint` clean. **Accepted caveat:** the two consumers keep intentionally different token sets (intent routing vs. gap-UI gating), now co-located and documented rather than merged — reconciling the *content* of those differences would change behavior/classifier semantics and is deferred to a future ticket (recorded in `keywords.ts`). |
| KD-12 | Audit-log write amplification: 2 rows per chat message per Space (shadow plans in metadata) | Low | v2.6b | Open |
| KD-13 | Repo hygiene: personal photos at root, `" 2"` Finder-duplicate dirs, uncommitted branch changes, `.env` docs name `ANTHROPIC_API_KEY` while code requires `OPENAI_API_KEY` | Low | v2.4.5 → v2.5 | **Reopened — partially resolved.** Original fixes hold (`5aba9cb`): personal jpegs removed, `.env.example` uses `OPENAI_API_KEY`, stray `signal-registry.ts` deleted. **But the duplicate-dir residue is back on disk** (empty, untracked — `git ls-files` shows 0 tracked files inside): now **19** `" 2"` Finder/cloud-sync duplicate dirs, spread beyond `lib/` into `app/api/spaces/[id] 2`, `app/api/spaces/invites 2`, `components/space/{widgets,sections} 2`, the six original `lib/ai/*`/`lib/providers/plaid 2` dirs, four `docs/initiatives/d2/* 2`, and four gitignored `.next/dev/* 2` cache dirs. Root cause is almost certainly cloud-sync (iCloud/Dropbox) on the `Documents` folder duplicating directories. The design-foundation working tree is **no longer uncommitted** — the tree is now effectively clean (4 untracked docs) and the Liquid card redesign is committed (§3 UI-1). Re-close in the v2.5 window: stop the sync duplication / gitignore the pattern, then remove the `" 2"` dirs |
| KD-14 | Scheduler entrypoint never invoked; `run-ai-advice`/`sync-crypto` stubs; `AiAdvice` has never had a write path | High (blocks v2.6b) | v2.6b entry | Open |
| KD-15 | UI transaction read paths ignore `visibilityLevel`: `lib/data/transactions.ts` (`getTransactions`/`getDebtTransactions`/`getInvestmentTransactions`) filter SAL on `status: ACTIVE` only, so BALANCE_ONLY/SUMMARY_ONLY accounts' rows reach dashboard pages and `app/api/accounts/[id]/transactions` — same defect class as KD-1, discovered during the KD-1 impact map (2026-07-02). Violates the `VisibilityLevel` enum contract ("BALANCE_ONLY — … no transactions"). Fix reuses `lib/ai/visibility.ts` | **Critical** | v2.4.5 | **Fixed & committed** 2026-07-02 (`98e3ab0`) — `getTransactions`/`getDebtTransactions`/`getInvestmentTransactions` and `app/api/accounts/[id]/transactions` now filter the SAL path on `TRANSACTION_DETAIL_VISIBILITY` (same predicate as KD-1); tests: `lib/data/transactions.privacy.test.ts` |
| KD-16 | Contradictory data-availability claims across intent paths: the AI produced a 24-month comparison, then claimed "only the last three months" for a semantically equivalent follow-up. Root concern: the active time window is re-derived per turn from message-text heuristics with **silent** failure modes — "since <year>" resolves to the closed calendar year, not year-to-present (`lib/ai/intent/classifier.ts:392-408`); the `MAX_EXPLICIT_WINDOW_DAYS` clamp is undisclosed; unparsed phrasings fall back silently to the 90-day default (`app/api/ai/chat/route.ts:289-313`); the model narrates context-selection artifacts as capability limits. Validator structurally blind (KD-2 membership caveat — all figures reconcile). Design record: `docs/investigations/KD16_INTENT_PATH_WINDOW_CONSISTENCY_PROPOSAL.md` | Medium-High | v2.6a (AI-5) | Open — logged 2026-07-02; scope + acceptance criteria in the design record |
| KD-17 | Category rollup sign asymmetry: per-category totals (window + monthly `byCategory`) aggregate `abs(signed net)` **including credits**, while `expenseTotal` sums debit rows only, so a credit classified into a spending category (systematically concentrated in `Other` via the Plaid mapper's default, `lib/plaid/syncTransactions.ts:118,134`) yields mathematically impossible figures — observed: Jan 2026 `Other $6,529.45` > total spending `$5,848.70`. Transaction-level proof: four Chase card-payment credits (+$9,500) categorized `Other` netted against $2,970.55 of debits (`docs/investigations/KD17_TRANSACTION_LEVEL_PROOF.md` + archived audit output). Side findings (fix in v2.5.5): `Fee` mapped but absent from `BANKING_CATEGORIES`; `Groceries` unreachable from the Plaid mapper; `Interest` debits inside `expenseTotal` but name-filtered from the categories line | **High** | v2.4.5 (absorbed 2026-07-02) | **Fixed & committed 2026-07-02 (`3988510`)** — universal debits-only `total` at both rollup sites (`lib/ai/assemblers/transactions.ts`), additive `CategorySpend.creditTotal` disclosure (`lib/ai/types.ts`), credits disclosed inline and the "≤ expenseTotal" invariant now **checked** at both serialization points (`checkSpendingCategoryInvariant`; dev throw / prod log+annotate) in `app/api/ai/chat/route.ts`. Drilldown unchanged — all three surfaces now agree by construction (Jan `Other` = $2,970.55 everywhere). Tests: `lib/ai/assemblers/transactions.kd17.test.ts` (26 cases: Jan-2026 shape, net-positive month, pure-credit month, invariant trip/tolerance, source tripwires). Validation: `tsc --noEmit` clean, `lint` clean, KD-1 + AI-4 suites still green. Checklist: `docs/initiatives/kd17/KD-17_IMPLEMENTATION_CHECKLIST.md`. Post-fix user verification: January categories reconcile to $5,848.70 exactly. **Accepted caveats:** historical transcripts will show smaller (correct) category figures; `Travel`/`Shopping` shift slightly where credits previously netted in; data-side miscategorization of payment credits remains (harmless post-fix; recategorization deferred to v2.5.5) |
| KD-18 | AI fabricates per-liability attribution of debt payments: `debtPaymentTotal` is a per-month scalar over source-side (checking) debit legs only; the summary query select discards account identity (`lib/ai/assemblers/transactions.ts:235-241`), no `byLiability` rollup exists, and the prompt presents monthly totals next to named liabilities with no attribution disclaimer — so a per-card question ("how much have I paid toward each card") yields an invented allocation (observed: ~100% attributed to one card, $0 to the other) that the membership validator structurally cannot catch (attribution, not figures). Destination attribution **exists deterministically in the DB** on card-side credit legs (`financialAccountId` + `debtSubtype`) but is partially miscategorized (KD-17 data finding) and never reaches any rollup. Same missing-dimension class affects transfers, per-card interest, per-account income/spending (watch list). Investigation: `docs/investigations/DEBT_PAYMENT_ATTRIBUTION_INVESTIGATION.md`; proof script: `scripts/kd-debt-attribution-audit.ts` | High | v2.4.5 guardrail (absorbed 2026-07-02); capability shipped 2026-07-05 (FlowType P5) | **Fixed & committed 2026-07-02 (`3988510`)** · **capability shipped 2026-07-05** — guardrail only, **generalized** per the approved refinement to every missing dimension (not just debt payments): named `ATTRIBUTION_DISCLOSURE` emitted once in the transaction section of `serializeContextBlock` (states the account/card/source/destination dimension is absent from context) + generalized `ATTRIBUTION_RULE` in `ADVISOR_PRINCIPLES` (serializes into both space and master prompts) forbidding any invented per-account/per-card/per-source/per-destination breakdown the membership validator structurally cannot catch. No schema, no aggregation, no new rollups, no UI. Tests: `app/api/ai/chat/attribution-guardrail.kd18.test.ts` (32 checks: disclosure exists, doctrine exists and is generalized, both serialized into both prompt builders, single-insertion so ordinary prompts unchanged, answer-first ordering, wording pinned). Validation: `tsc --noEmit` clean for the changed files, `lint` clean, KD-17/KD-1/AI-4 suites still green. **Response-doctrine refinement committed 2026-07-02 (`ea223af`)** — the rule was refusal-first (model led with "I can't provide spending by card" and discarded truthful totals); it is now **answer-first**: (1) answer every deterministic portion the context does contain (exact period total + any breakdown along a present dimension — category/month/merchant), (2) then disclose the missing per-account/card/source/destination dimension once, (3) offer the nearest truthful alternative — while still forbidding any invented split. Prompt-text only (no schema/aggregation/context-window change); test adds ordering assertions so a regression to refusal-first fails. **The per-liability capability itself has now shipped** (2026-07-05, FlowType P5): Slice 3 delivered the deterministic per-liability debt-payment rollup from destination-side card-credit legs (`lib/debt.ts` `totalDebtPaid` + per-card rollup) and Slice 6 surfaced it through the chat serializer (per-liability block) with the corresponding guardrail relaxation — the AI now answers per-card payment questions from real destination attribution instead of disclosing a missing dimension. See the FlowType ledger entry in §3. Checklist: `docs/initiatives/kd18/KD-18_IMPLEMENTATION_CHECKLIST.md` |
| KD-19 | UI account read path ignores `visibilityLevel`: `lib/data/accounts.ts` `getAccounts()` filters SAL on `status: ACTIVE` only (no visibility branch) and returns full `institution`, real `name`, `creditLimit`, `debtSubtype`, `interestRate`, `minimumPayment`, and `debtProfile` for **every** active link — so a BALANCE_ONLY-shared account's identifying metadata leaks to every dashboard page (`accounts`/`banking`/`credit`/`investments`/`holdings`/main `page.tsx`) of the Space it is shared into. Sibling leak in the same function: `getHoldings()` FA branch (`lib/data/accounts.ts`) returns individual positions (symbol/name/quantity/price) for BALANCE_ONLY/SUMMARY_ONLY links, which must expose aggregate value only. Same defect class as KD-1/KD-15 (a read surface disagreeing with the canonical visibility rule), but for account **metadata + positions** rather than transaction rows. The AI accounts/holdings assemblers and `app/api/spaces/[id]/accounts/route.ts` sanitize correctly; only the data layer did not. Reachable: POST `/api/spaces/[id]/accounts/share` permits BALANCE_ONLY. Discovered during the v2.5-A seam-closure investigation (`docs/investigations/V25A_SEAM_CLOSURE_INVESTIGATION.md` §4-R1/R2). Not covered by the pre-existing two-user test | **High** | v2.5 (v2.5-A Phase 1) | **Fixed & committed (v2.5-A Phase 1, `232f7f7`)** — `getAccounts()` now branches per-link on visibility and sanitizes non-FULL links via `sanitizeForBalanceOnly` (`lib/account-privacy.ts`), exposing balance total only (generic owner-prefixed name, redacted institution/debt metadata); `getHoldings()` FA branch now filters positions on FULL-only detail. Shared gate `grantsAccountDetail()` added to `lib/ai/visibility.ts` (reuses `TRANSACTION_DETAIL_VISIBILITY` so the data layer and AI assemblers can never diverge); fails closed for SUMMARY_ONLY/PRIVATE/SHARED. Additive only — **no schema change, no WAS/Account retirement**. Test: `scripts/test-visibility-two-user-space.impl.ts` extended to assert `getAccounts()` metadata redaction (institution/name canaries), no balance over-redaction, REVOKED exclusion, and `getHoldings()` position redaction. Validation: `tsc --noEmit` clean, `lint` clean, two-user proof green. **Accepted caveat:** individual (non-aggregated) sanitized rows may render multiple generic "…'s Checking Account" cards on the personal dashboard; the shared-Space accounts tab still aggregates via `normalizeSharedAccounts`. WAS retirement is complete (v2.5-A Phase 4c, 2026-07-03, `e17c699` — see D3); legacy-`Account` retirement (Phase 5) is deliberately deferred pending separate approval |

## 8. Parked ideas

The roadmap ends at launch. These are deliberately parked, not forgotten. Each lists its unpark condition.

| Idea | Why parked | Unpark condition |
|---|---|---|
| Marketplace / SpaceTemplate (matrix D9) | Zero users; no demand signal; distracts from launch | Real users requesting templates post-launch |
| Internal-ops Spaces (matrix D12) | Internal team doesn't exist; putting privileged ops data inside customer tenancy would weaken the codebase's strongest boundary. True dogfooding = run company finances in a normal BUSINESS Space (costs nothing, start now) | Internal headcount whose workflows demonstrably outgrow the Admin Console; then implement via `isInternal` + separate authz gate per matrix D12 |
| ProviderAdapter abstraction | D2 Step 5 investigation correctly concluded a generic interface before a second provider is speculation | A second sync provider committed (this also unparks the `Connection` cutover) |
| PublishedAccountView | Public trust boundary over financial data; private boundary not yet hardened (KD-1 exists) | External security review of the private sharing boundary passed |
| Second sync provider / investment transactions / wallet providers | Launch does not require them; Plaid-only launch is fine | Post-launch, ranked by usage evidence |
| Agents / automation workflows | An assistant must first never misstate a number when asked before it acts unprompted or autonomously | Post-launch + validator track record |
| Billing/payouts/messaging/support tables (D10 list) | Ratified out of Phase 2 | Billing only: lifts at v3.0 |
| Decimal/int-cents money migration | High-churn schema period; migration is large | Plan during v2.5; execute post-v2.6b |
| Liquid / real-refraction glass material (`liquid-glass-web-react`, `@ogtirth/liquid-glass-oss`) | **Evaluated and rejected** (2026-07-03) — the library is a *content* lens (`feDisplacementMap` on `SourceGraphic`), not a *backdrop* lens, so it warps a surface's own text/icons or the Earth image, never the backdrop behind foreground glass; backdrop refraction (`backdrop-filter: url(#svg)`) is Chromium-only and already rejected. Atlas stays on the CSS material simulation for the backdrop. **Note (2026-07-04):** only the *backdrop*-refraction direction is parked/rejected — the *content-lens* Atlas Liquid card components shipped and are committed (see §3 UI-1); the material was **vendored** into `components/atlas/vendor/liquid-glass/` rather than kept as npm deps, and the working tree is clean, so the earlier "two npm deps uncommitted pending prune" note no longer applies | Not a product direction (backdrop refraction). Would only unpark if a cross-browser true-backdrop-refraction primitive becomes viable |

## 9. Documentation map

| Looking for… | Read | Nature |
|---|---|---|
| Current state, roadmap, defects, initiative status | **`STATUS.md`** (this file) | Living — the only current-state authority |
| What was decided and why (D1–D14) | `docs/architecture/PHASE_2_DECISION_MATRIX.md` | Immutable decision record |
| Architecture baseline at Phase 2 start | `docs/architecture/PHASE_2_ARCHITECTURE_FREEZE.md` | Immutable; schema line refs are dated |
| Deep-dives behind decisions | `docs/initiatives/dN/`, `docs/investigations/` | Immutable point-in-time investigations |
| What shipped in an initiative | Closeout/implementation reports inside `docs/initiatives/dN/` | Immutable |
| How to deploy / operate | `docs/operations/DEPLOYMENT.md`, `HYDRATION_RULES.md` | Living operational references |
| Historical status snapshots | `docs/architecture/PHASE_2_CANONICAL_ROADMAP_AUDIT.md`, `docs/operations/PROJECT_STATE.md`, `ROADMAP.md` | Superseded — historical only, do not trust for status |
| Bug post-mortems | `docs/bugfixes/` | Immutable |
| Release notes | `docs/releases/` | Immutable per version |

## 10. Documentation cleanup recommendations

No documents were edited or deleted. Recommended dispositions:

| Document | Disposition | Rationale |
|---|---|---|
| `ROADMAP.md` (root) | **Redirect** | Frozen at v1.0; milestones already shipped/superseded. Replace body with a pointer to STATUS.md; move run instructions to README |
| `docs/operations/PROJECT_STATE.md` | **Archive** | Dated 2026-06-11; contradicts live system (calls AI chat "mock"). Move to `docs/archive/` with superseded header |
| `PHASE_2_CANONICAL_ROADMAP_AUDIT.md` | **Supersede** (keep in place) | Status table stale within days (D14 "Not Started" is wrong; D4 status wrong). Add point-in-time header; method section remains valuable |
| `PHASE_2_DECISION_MATRIX.md` | **Keep** (immutable) | Sole D-number authority. Add header: decisions only, not status |
| `PHASE_2_ARCHITECTURE_FREEZE.md` | **Keep** (immutable) | Add point-in-time header; line-number citations and some model claims (e.g., Holding FK) predate D11 |
| `docs/initiatives/d2/D2_ROADMAP.md` | **Supersede after fix** | Apply the 5 doc-only corrections from `V24_COMPLETION_PLANNING_INVESTIGATION.md` §1, then freeze; residuals tracked here |
| `docs/investigations/D6_3*.md` | **Keep** (do not move/rename) | Alias table (§4) maps them to AI-3 |
| `docs/README.md` | **Keep, trim** | Folder index only; remove status language; point here |
| `docs/archive/` gitignore status | **Decide** | An untracked archive isn't an archive. Recommend tracking (excluding images with personal content) |
| Stale code comments (`context-builder.ts` "Slice 1 has none / no detectors exist"; `visibility.ts` citing uncommitted `scripts/audit-visibility-levels.ts`; `transactions.ts` citing missing `docs/initiatives/kd15/…`) | **Done** | Superseded 2026-07-02 in the status/comment truth-up — comments now reflect reality; `space-account-link.ts` "nothing reads SAL yet" comment already gone |
| Root-level personal photos, `" 2"` duplicate dirs | **Archive/remove** | KD-13, v2.4.5 hygiene |

## 11. Executive summary

**Architecture maturity: high for its stage.** The expensive-to-retrofit boundaries — tenancy, encryption, auth chokepoints, the LLM provider seam — are correct and consistently enforced. The AI layer's deterministic-first design (the model narrates pre-computed, provenance-carrying facts; it never calculates) is a genuine differentiator executed above industry norm.

**Launch readiness: not yet, by design.** The gap is not features — it is verification. Thin test coverage, invariants still asserted in comments, and several migration seams open at once. The critical privacy defects (KD-1, KD-15) are closed, LLM output validation is now live-enforcing (KD-2) rather than observational, and the two AI financial-context honesty defects are fixed & committed — figure correctness via a checked category invariant (KD-17) and fabricated-attribution prevention via a generalized non-attribution guardrail (KD-18) — but the broader enforcement discipline is not yet fully systematized. v2.4.5 exists precisely to convert prose guarantees into checked ones, and the roadmap gates every subsequent phase on it.

**Largest strengths:** deterministic AI assessment pipeline; Plaid failure-mode resilience; migration discipline; security architecture fundamentals.

**Largest weaknesses:** verification debt; concurrent seam count; solo bus factor; no operational substrate for scheduled intelligence yet.

**Biggest risks:** documentation reverting to fragmentation if this file is not maintained; residual false-positive tuning on the now-live validator before `block` can be trusted as default. (Items previously headlining this list are now closed — the BALANCE_ONLY leak via KD-1/KD-15, the non-atomic SAL writes via KD-4, HOME uniqueness under concurrency via KD-5, and *live* output validation via KD-2 — all fixed and committed. The prior "ambient features speaking unprompted before live validation exists" risk is retired: the validator is enforcing, and v2.6b ambient behavior still gates on it — and, as of the 2026-07-02 roadmap revision, on the v2.6a conversation layer as well.)

**Recommendation:** v2.4 is closed and `v2.4.5` tagged (`6b517fa`) — the two absorbed additions landed (KD-17 and the KD-18 honesty guardrail — both correctness defects, which is what the gate is for; **both fixed & committed, `3988510`**; the KD-18 *capability* is ratified into v2.5.5 FlowType, not absorbed), with the v2.4.5 verification debt (named test suites, observability counters, max-50 copy) carried forward into the now-open v2.5 window; keep v2.5 as seam-closure plus design foundation; make the data semantically sound in v2.5.5; make conversations coherent in v2.6a; let v2.6b earn ambient behavior behind both the validator and the conversation layer; launch at v3.0 with zero new surface. The AI evolution ladder: **v2.4.5 makes every answer honest → v2.5/v2.5.5 make the data singular and semantically sound → v2.6a makes conversations coherent → v2.6b earns the right to speak unprompted → v3.0 sells it.** Each phase's exit criteria are the next phase's entry criteria. (Roadmap revision approved 2026-07-02: `docs/ROADMAP_REVISION_PROPOSAL_2026-07.md`.) Everything else stays in §8 until the market says otherwise.
